# X4 Forge Studio 0.0.65 OpenVSX recovery release

Status: VERIFIED
Lane: FULL
Date: 2026-08-07

## PLAN

- **Bounded unit:** repair the existing deterministic changelog generator's EOF contract, cut stable extension
  version `0.0.65` with the complete already-verified native Forge capability notes, publish its exact inspected VSIX
  once to OpenVSX, verify public byte/hash parity, then commit and push the exact release checkpoint.
- **Assumptions and unresolved facts:** OpenVSX accepted `0.0.64` but still reports public latest `0.0.63` and exact
  `0.0.64` HTTP `404` at `2026-08-07T07:54:53Z`. The occupied version must never be republished. The registry does
  not expose publisher scan disposition; `0.0.65` is the unique next monotonic stable version.
- **Authoritative references:** `AGENTS.md`; `vscode-extension/PUBLISHING.md`; the existing
  `vscode-extension/scripts/gen-changelog.mjs`; package/lock/release-note manifests; package inspector and staged-app
  probe; blocked `0.0.64` plan/evidence; public OpenVSX namespace `x4forge.x4-forge-studio`.
- **In scope:** exact EOF repair and selftest; package/lock bump to `0.0.65`; comprehensive plain-language `0.0.65`
  notes; deterministic changelog regeneration; root/extension build, fresh staging, stable package, inspection and
  probe; one authorized OpenVSX upload; public metadata/download parity; durable close; exact Git/GitHub synchronization.
- **Out of scope:** changing verified Forge runtime behavior, modifying game/mod data, Microsoft Marketplace,
  pre-release channel, replacing or deleting `0.0.64`, alternate release tooling, standalone app, end-user CLI, or
  beginning W3B1b before this checkpoint closes.
- **Affected surfaces:** `vscode-extension/scripts/gen-changelog.mjs`, `package.json`, `package-lock.json`,
  `release-notes.json`, generated `CHANGELOG.md`, ignored build/package artifacts, release evidence, roadmap/handoff,
  exact feature files already awaiting the publish-before-commit checkpoint, and owner issues `#9/#19/#20` after push.
- **Risks and authorization boundaries:** OpenVSX publication irreversibly occupies a version; the owner explicitly
  authorized public release. The token may exist only in memory and must never be printed or persisted. Any red
  generator, secret, stable-channel, package, probe, public-parity, precommit, or staged-scope gate stops publication
  or close. Existing unrelated dirty paths remain owner-owned.
- **Rollback/checkpoint:** before upload, abandon a red `0.0.65` candidate and retain public `0.0.63` plus immutable
  local `0.0.64` evidence. After a successful upload, never retry `0.0.65`; wait for exact public disposition. The
  installed rollback remains the validated `0.0.63` busy-liveness VSIX.
- **Evidence locations:** `vscode-extension/evidence/0.0.65-release-validation.md`, generated changelog, exact local
  and public archive hashes, `ROADMAP.md`, `SESSION-HANDOFF.md`, and synchronized GitHub marker readback.

## ACCEPTANCE CONTRACT

1. Native routing uses exact `luna_executor` / `gpt-5.6-luna` / max effort / no parent-turn fork. The worker owns only
   the generator and four release metadata outputs and reports its exact files/tests.
2. `buildChangelog()` emits LF markdown ending in exactly one `\n`, never `\n\n`; the entrypoint writes those exact
   bytes without appending another newline. Selftests cover the terminal-byte contract and all prior behavior.
3. Package, lock top-level, and lock root versions equal `0.0.65`. Curated `0.0.65` notes comprehensively describe
   the already-verified native features without internal source-product names. Generated changelog starts with the
   exact version/date and ordered bullets, has one terminal LF, and passes full `git diff --check`.
4. Root production build, changelog selftest/generation, fresh staged-app secret/native guard, extension build, stable
   VSIX packaging, archive inspection, and staged-sidecar probe all pass in authoritative order.
5. The package has no pre-release marker and is uploaded once as `x4forge.x4-forge-studio v0.0.65`. Neither
   `0.0.64` nor a failed/occupied `0.0.65` is republished.
6. OpenVSX latest and exact-version metadata report `0.0.65`; its public archive downloads successfully and equals
   the inspected local candidate in byte count and SHA-256.
7. Final precommit passes after all close records. Exact staging excludes every unrelated dirty path; commit/push
   proves `HEAD == origin/main == remote main`; GitHub issues remain open with exactly one synchronized marker each.

## REQUIRED VALIDATION

- Generator: focused selftest plus direct terminal-byte assertions against fixture and real generated changelog.
- Metadata: JSON parse, version equality, ordered bullet equality, forbidden-name scan, stable-channel readback.
- Package: root build; changelog; stage-app; extension build; stable VSCE package; inspector; staged-app probe; hash.
- Negative paths: preserve `0.0.64`; reject extra EOF blank, pre-release marker, secret, manifest mismatch, inspector or
  probe red, public/local mismatch, broad staging, and any game/mod write.
- Publication: load `OVSX_PAT` from gitignored `.env.local` into memory only; upload exact archive once; bounded
  latest/exact/download verification.
- Close: review every requirement, update evidence/records/AAR, final precommit, exact staged diff, commit/push/remote
  parity, GitHub marker readback.

## BASELINE

- `HEAD == origin/main == c334127888368993cf350e13839376a0d44507d4`; all W3B1a and release work remains
  intentionally uncommitted under publish-before-commit. Unrelated dirty paths are enumerated in
  `SESSION-HANDOFF.md` and must remain untouched.
- W3B1a is installed and VERIFIED `5/5`; synchronized feature precommit previously passed in `533.6s`.
- Local manifests and blocked archive are `0.0.64`; archive is `18,098,219` bytes at SHA-256
  `925616AD141D45AC75E8AD90E5A21FD73F0DE0DADC6CA0B5B23C7D4C11F03057`.
- Public OpenVSX latest is `0.0.63`; exact `0.0.64` is HTTP `404` as of `2026-08-07T07:54:53Z`.
- Reproduced generator defect: `buildChangelog()` leaves a blank array entry after the final release and the
  entrypoint appends another newline, producing terminal bytes `0A0A`. Clean working changelog currently ends `0A`.

## RECONCILE

- Reuse the current generator and selftest. Do not introduce another changelog writer or package pipeline.
- The defect is localized to the pure builder/entrypoint boundary. The correct owner is
  `vscode-extension/scripts/gen-changelog.mjs`; release metadata remains declarative input.
- `0.0.64` is immutable evidence, not a candidate for replacement. Recovery must be a new stable `0.0.65` archive.
- Capability-map delta: none; this unit repairs release determinism and republishes already-verified capabilities.

## IMPLEMENT

- Native `luna_executor` routing used exact `gpt-5.6-luna`, effort `max`, and no parent-turn fork. The worker changed
  only the five declared generator/metadata paths and reported no blocker.
- `buildChangelog()` now normalizes LF output, removes terminal newline runs, appends exactly one LF, and writes those
  exact bytes. Four focused EOF/determinism cases extend the existing selftest to `12/12`.
- Package/lock moved to `0.0.65`; curated notes contain `11` comprehensive user-facing bullets; generated changelog
  starts with `0.0.65 — 2026-08-07`, matches the curated order exactly, ends in one LF, and contains no CR.
- The existing build/stage/package/inspect/probe pipeline produced stable
  `vscode-extension/x4-forge-studio-0.0.65.vsix`. It was uploaded to OpenVSX exactly once and installed into the real
  Antigravity host after public byte/hash parity passed.

## VALIDATE

- Generator selftest `12/12`; direct metadata/version/order/EOF readback PASS; product-copy scan `0` banned mentions;
  focused release diff check PASS.
- Root build PASS (`1,828` modules); fresh stage-app PASS (`169` runtime packages, native binding present, no secrets);
  extension build and stable package PASS; inspector PASS (`2,091` entries / `61,446,861` unpacked bytes); staged
  sidecar probe `16/16` PASS.
- Exact local candidate: `18,098,264` bytes, SHA-256
  `ACBF40475A0AB55AA269E5728FE2B0927C22C9B9CC1F38F12AAD473A1F392D21`; embedded version `0.0.65`, no pre-release
  marker, clean changelog bytes, and critical bundle hashes matching the installed-validated feature build.
- Full `npm run precommit:check` PASS, exit `0`, `492.9s`, final `[precommit] OK`, including E2E verdict `54/54`,
  receipt coverage `82/52`, writer/capability/MCP policy, typecheck, mirror, tripwire, and product-copy gates.
- Final synchronized precommit after durable close records PASS, exit `0`, `465.4s`, terminal `[precommit] OK`;
  the commit hook repeats this gate against the exact staged checkpoint.
- Pre-upload exact `0.0.65` endpoint was HTTP `404`. Publication returned
  `Published x4forge.x4-forge-studio v0.0.65`, exit `0`, at `2026-08-07T08:19:28.9813238Z`; no retry occurred.
- OpenVSX latest/exact metadata reached `0.0.65`/HTTP `200`. Its public download is `18,098,264` bytes at the exact
  local SHA-256 above; public parity is `VERIFIED`.
- Antigravity reports `x4forge.x4-forge-studio@0.0.65`. After a real host reload, Forge `v1.0.428` rendered with
  Mission Director `1507`, AI-script `1408`, and script-property `2333` sources loaded. Installed critical hashes
  match the package; the managed installed sidecar on `:52634` returned receipt selftest `25/25` and the required
  bulk-apply operation header. `config.json` remained byte-identical.
- Negative paths PASS: immutable `0.0.64` was neither changed nor republished; no pre-release, secret, manifest drift,
  public/local mismatch, game/mod write, or unrelated package input occurred. One broad supplemental secret regex
  overmatched dependency test-data names; the authoritative inspector and refined package-root scan found zero.
- Evidence: `vscode-extension/evidence/0.0.65-release-validation.md` and
  `vscode-extension/evidence/0.0.65-installed-antigravity-forge-v1.0.428.png`.

## REVIEW

- Requirements 1-6 are done and evidenced. Requirement 7 is closed by the exact release commit/push and post-push
  `#9/#19/#20` marker readback containing this record; every issue remains open because W3B1/W3/the program remain
  partial.
- Fresh-eyes review confirmed the archive is stable, version-coherent, changelog-clean, secret-clean, and byte-equal
  to the public download. Installed-source hashes, live schema/receipt behavior, and rendered UI were proved
  independently rather than inferred from package inspection.
- Deliberately unchanged: Forge runtime behavior beyond the already-verified W3B1a checkpoint, game/mod state,
  `0.0.64`, pre-release/Microsoft channels, and every unrelated dirty owner path.

## CLOSE

- Status: `VERIFIED` for stable OpenVSX `0.0.65`, public archive parity, and installed Antigravity validation. W3B1a
  remains `VERIFIED` at `5/5`; W3B1/W3 and the overall program remain `IN_PROGRESS / PARTIAL`.
- Capability-map delta: none; publication exposes already-verified native capabilities and repairs release
  determinism without adding a new Forge capability.
- Commit title: `release: publish X4 Forge Studio 0.0.65`.

## AAR

- Triggers: `0.0.64` was accepted but withheld; its post-upload EOF mismatch required monotonic recovery. The first
  coordinator pre-publish JSON projection repeated the known PowerShell empty-root-key error before the hashtable
  correction. A supplemental secret regex overmatched dependency test-data names. The first reload shortcut did not
  expose the command palette, so the observed View-menu path was used. All failed attempts were non-mutating or
  recovered before acceptance.
- Sustain: immutable-version/no-republish discipline; exact native Luna ownership; generator terminal-byte tests;
  build/stage/inspect/probe before upload; public byte/hash parity; and independent installed-byte, process, API, and
  rendered-host proof.
- Improve work/approach: treat registry upload acknowledgment, public activation, local archive identity, and
  installed-host identity as four separate gates. Run the changelog EOF assertion before occupying a version.
- Improve tools: parse `package-lock.json` with `ConvertFrom-Json -AsHashtable` because its root package key is empty;
  scope supplemental secret checks to package-root contract paths and keep the authoritative inspector primary.
- Highest-risk evidenced weakness: W3B1b-d mutation owners still lack complete native receipt authority; this
  release exposes W3B1a but does not close the remaining transaction gaps.
