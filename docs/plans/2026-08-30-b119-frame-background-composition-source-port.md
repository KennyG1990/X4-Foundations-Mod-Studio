# B119 Frame Background Composition Source Port

Status: `PARTIAL / SOURCE + PACKAGE VERIFIED / CORRECTED PACKAGE UNMOUNTED`; the source-proven local `Helper.scaleFont` wrapper port, strict configured corpus, full static/runtime matrix, production build, and isolated 0.0.70 VSIX pass. The attempted same-version installed-extension replacement was blocked by Antigravity's active native-module lock and safely reverted to the prior byte-identical installed payload. New mounted pixels, three-menu visual parity, keep-outs, AI Influence reconstruction, OpenVSX publication, and current in-game comparison remain pending. Parent B119 remains `PARTIAL / Not verified in game`.

Task: Port X4 9.00 frame texture activation and backdrop semantics into the existing source-first preview, then remove the false full-frame source-composition outline from the retained `pipeline_test` fixture without hiding real diagnostics.

Lane: `FULL`

## PLAN

- Bounded unit: extend the existing CallModel -> LayoutProgram -> Scene -> PaintPlan -> Canvas path with the three shipped frame texture layers (`background`, `background2`, and `overlay`) and the separate `blurBackground` compositor fact. The browser must paint no frame texture when the corresponding icon is empty. The retained `pipeline_test` preview must therefore stop drawing a panel-sized frame boundary that X4 did not draw.
- Assumptions:
  - The configured X4 9.00 unpacked corpus is authoritative for Lua-side layout and widget composition.
  - C++ frame acceptance, icon material lookup, the live game scene, and blur pixels remain outside static proof.
  - `pipeline_test` is the first causal fixture because its exact deployed Lua and two X4 screenshots already exist.
- Unresolved facts:
  - Non-empty icon IDs cannot be rasterized faithfully until the configured corpus owner can map the exact icon/material asset and transfer behavior.
  - `addFrameBorder`, frame brackets, standard-button texture paint, and game-scene blur rasterization are separate shipped paths and remain later units.
- Authoritative references:
  - `F:\Downskies\x4unpackersuiteV1\X4 unpacked 9.00\ui\addons\ego_detailmonitorhelper\helper.lua`, SHA-256 `D24A08B8DA9F2C972794B60ACB48AE36F38CB026C991249DAB9F1164272D4DF2`:
    - frame `blurBackground = true` default at line 3133;
    - `frametextureproperty` defaults at lines 3446-3458;
    - descriptor creation at lines 3716-3740;
    - frame texture setters at lines 3931-3946;
    - compositor property at line 4055;
    - descriptor layers at lines 4087-4089.
  - `F:\Downskies\x4unpackersuiteV1\X4 unpacked 9.00\ui\widget\lua\widget_fullscreen.lua`, SHA-256 `420AFBA33D925A7B55F2A82AB12773DF04826EF588317010D209B249DE7BAED1`:
    - frame texture element family at lines 462-471 and 9702-9763;
    - exact activation, size fallback, tint/alpha, centering, rotation/scale, and empty-icon deactivation at lines 16945-17024;
    - brackets and borders begin only after that texture path.
  - Retained deployed source `dev-docs/b119-x4-ui-pipeline-smoke/package/pipeline_test/ui/pipeline_test.lua`, SHA-256 `C1D9CD8580C6175E95C543259A2AB19F8B463282BF48B2229EB6013D6052718E`.
- Existing infrastructure reused:
  - `x4UiCallModel` source-located tracked receiver and color-expression owners;
  - `x4UiLayoutProgram` descriptor-fact/default/source-pin authority;
  - `x4UiScene` immutable provenance, completeness, and diagnostic-link model;
  - `x4UiPaintPlan` separation of geometry, glyph, diagnostic, and keep-out layers;
  - `x4UiCanvasRenderer` explicit `diagnostic-map | source-composition` presentations;
  - existing configured-corpus loader, receipt identity, preview pipeline, and permanent game-truth wording.
- In scope:
  - recognize exact tracked-frame `setBackground`, `setBackground2`, and `setOverlay` calls and their ordered mutations;
  - replace the advertised `createFrameHandle` visual `backgroundID/backgroundColor` fiction with the shipped complex texture-property route while preserving compatibility only where a consumer demonstrably requires raw source visibility;
  - project source-pinned defaults and exact known descriptor facts for icon, color, width, height, rotation rate/start/duration/interval, initial scale factor/duration, and glow factor;
  - project `blurBackground` independently with its shipped default `true` and explicit dynamic-game-backdrop limitation;
  - classify an empty icon as an exact inactive visual layer and issue no invented fill, texture, or visual boundary for it;
  - keep nonvisual Helper-runtime/data-flow uncertainty in receipts and diagnostics without converting it into panel-sized source-composition paint;
  - keep non-empty texture IDs explicit and partial until an exact configured-corpus texture is accepted;
  - add causal end-to-end assertions over the real `pipeline_test` source and the public Preview -> Scene -> Paint -> Canvas path.
- Out of scope:
  - C++ acceptance proof, new X4 launch/deploy, live game writes, or claims beyond the retained in-game screenshots;
  - icon/material decoding, texture atlas lookup, UV behavior, animation rasterization, glow parity, brackets, frame borders, standard close-button paint, or dynamic game-scene blur pixels;
  - a new renderer, alternate preview route, generic CSS approximation, guessed panel fill, or global diagnostic suppression;
  - AI Influence reference reconstruction and keep-out changes.
- Likely owned implementation/test paths:
  - `src/lib/x4UiCallModel.ts` and `src/lib/x4UiCallModel.selftest.ts`;
  - `src/lib/x4UiLayoutProgram.ts` and `src/lib/x4UiLayoutProgram.selftest.ts`;
  - `src/lib/x4UiScene.ts` and `src/lib/x4UiScene.selftest.ts`;
  - `src/lib/x4UiPaintPlan.ts` and `src/lib/x4UiPaintPlan.selftest.ts`;
  - `src/lib/x4UiCanvasRenderer.selftest.ts` and `src/lib/x4UiPreviewPipeline.selftest.ts`;
  - `src/lib/x4UiCanvasRenderer.ts` only if a causal test proves PaintPlan cannot express the correction through the existing presentation contract.
- Explicitly forbidden paths: all mod/game/config/corpus files; `src/components/X4UiSourceEditor.tsx`; unrelated dirty files; generated output; release/version files; GitHub/Notion/Drive records until the local candidate passes review.
- Risks and authorization boundaries:
  - A too-broad completeness change could hide genuine unavailable geometry. Every suppression must be tied to exact frame-surface applicability and leave diagnostic metadata intact.
  - A guessed non-empty texture paint would create a more persuasive false preview. It must remain partial.
- No external write is authorized in implementation. Commit/push/promotion still require the acceptance gates. The user's standing bookkeeping request authorizes the parent to report truthful `PARTIAL` progress and blockers in the existing owner records before promotion; this is not source or release acceptance.
- Rollback/checkpoint: HEAD/origin baseline `bd38ec6ca52fedc0db9e98be8e27be5c07b00b47`; revert only the explicit B119 frame-composition commit or restore the frozen owned-file preimages below. Retained screenshots/receipts and unrelated dirty paths are immutable.

### Acceptance criteria

1. The public call model emits ordered, source-located records for all three exact frame setters only on a tracked frame receiver; wrong receiver, dynamic receiver, malformed option table, branch/loop ambiguity, and post-display mutation stay explicit gaps/refusals.
2. The frame property family matches shipped Helper 9.00: `createFrameHandle` owns simple frame properties, while texture color/icon/size/animation/glow facts come only from the corresponding complex texture layer.
3. LayoutProgram carries all three layers as immutable closed-domain descriptor evidence and applies setter calls in source order. Omitted layer calls retain the exact `frametextureproperty` defaults.
4. `blurBackground` is independently source-projected, defaults to `true`, and is never interpreted as a panel fill, texture ID, border, or evidence that browser pixels match the live game scene.
5. Scene exposes all three frame layers with explicit `inactive | active-unresolved | active-source-known` applicability, exact descriptor provenance, and a separate backdrop/compositor state. Empty icon is `inactive` even when the frame has unrelated nonvisual gaps.
6. PaintPlan emits no fill and no frame-sized visual diagnostic for an exactly inactive frame texture layer or the unavailable dynamic backdrop. The same gaps remain machine-readable in Scene/Paint receipts and product diagnostics.
7. A non-empty icon with no exact accepted texture asset remains partial and visibly diagnosable without fabricated interior pixels. `diagnostic-map` remains unchanged and source-composition retains boundaries for genuinely visual geometry/paint uncertainty.
8. The real retained `pipeline_test` source proves all three frame texture layers inactive, `blurBackground=true`, and no panel-sized frame boundary in source-composition at both retained profiles. Each profile must traverse public Preview -> Scene -> Paint -> Canvas without fixture normalization. Identify table/button/editbox/glyph content and inspect the complete frame-boundary trace, not a five-operation window. Permanent `Not verified in game` state remains.
9. Positive fixtures cover each setter, explicit and default properties, source-order replacement, zero width/height fallback metadata, symbolic/literal color provenance, and non-empty unresolved texture applicability.
10. Negative fixtures cover wrong receiver, dynamic icon/properties, malformed values, inherited/accessor/proxy pollution, forged descriptor members, mutation after issuance, stale source/corpus identity, and attempts to convert blur or empty icons into paint.
11. Existing focused suites, typecheck, exact-file lint, configured X4 UI corpus, aggregate B119 matrix, oracle sweep, build, serial E2E, precommit, graph refresh, and process/port containment pass before promotion.
12. The close remains `PARTIAL / Not verified in game`; no result in this unit may claim icon/material parity, blurred-game-scene parity, or universal X4 acceptance.

### Required validation and negative path

- Causal fail-first tests before production repair, then final:
  - `node node_modules/tsx/dist/cli.mjs src/lib/x4UiCallModel.selftest.ts`;
  - `node node_modules/tsx/dist/cli.mjs src/lib/x4UiLayoutProgram.selftest.ts`;
  - `node node_modules/tsx/dist/cli.mjs src/lib/x4UiScene.selftest.ts`;
  - `node node_modules/tsx/dist/cli.mjs src/lib/x4UiPaintPlan.selftest.ts`;
  - `node node_modules/tsx/dist/cli.mjs src/lib/x4UiCanvasRenderer.selftest.ts`;
  - `node node_modules/tsx/dist/cli.mjs src/lib/x4UiPreviewPipeline.selftest.ts`.
- `npm run typecheck`; zero-warning ESLint over exact owned source/test paths; `git diff --check`; source/hash/forbidden-owner scans.
- Two retained preview profiles and the mounted SourceEditor public route, including a pixel/operation census proving the false full-frame boundary is absent while actual content remains.
- `npm run test:x4-ui-corpus` against the configured unpacked corpus, full B119 aggregate, `node scripts/oracle-sweep.mjs`, `npm run build`, verdict-parsed serial `npm run test:e2e`, and `npm run precommit:check`.
- Negative-path result must prove diagnostic-map is unchanged and source-composition still exposes a non-empty unresolved texture without painting guessed pixels.
- Teardown must prove X4 count zero, ephemeral test stack stopped, live workspace unchanged, and no new worker/test process retained.

### Evidence locations

- Plan and close: this file plus the parent B119 plan and `SESSION-HANDOFF.md`.
- Retained profile receipt: `dev-docs/b119-x4-ui-pipeline-smoke/receipts/profile-2560x1440-uiScale-1.json`, SHA-256 `844FB4B62F82A0500CCE0E10202944D605205AF1C280F6AE734F6F583DA6B5E0`.
- Retained X4 screenshots:
  - `dev-docs/b119-x4-ui-pipeline-smoke/pixel-comparison-20260828/x4-current-2544x1353.jpg`, SHA-256 `570C5DF646A86FCA208C4178E9C73FCED2D7A39FE24352D595152003F3C6411C`;
  - `dev-docs/b119-x4-ui-pipeline-smoke/in-game-20260828/second-profile/panel-open-1920x1080.jpg`, SHA-256 `4518B47E6D6B21E004DAFD16D61A2B71FFF68E93B21BBF96673FE999249E7D12`.
- Reproduced Forge mismatch: `dev-docs/b119-x4-ui-pipeline-smoke/pixel-comparison-20260828/forge-source-preview-canvas-2560x1440.png`, SHA-256 `F2D23BB586ACE3282B1612464C2325A2A1826AE62A802460F7BDDC2AEE0DABC3`.
- New causal/final machine receipts: `dev-docs/b119-x4-ui-pipeline-smoke/receipts/frame-composition-source-port/` after issuance; generated test output remains ignored unless promoted deliberately.
- Existing external status projections: GitHub #41, Notion page `3b84618e-d15b-8190-821e-c0eb96f43d5a`, Drive Doc `17VLaIsT499KHg7zg30hOyLaBXB0-9jlrX3dQ63s3dtE` tab `t.0`. Partial checkpoint updates must name the missing gates and cannot claim promotion.

## BASELINE

- Revision/version: HEAD and `origin/main` are both `bd38ec6ca52fedc0db9e98be8e27be5c07b00b47`; staged index empty.
- Existing changes: only pre-existing unrelated documentation, Discord/data removals, VS Code release work, screenshots/media, W3B1 records, and scratch names are dirty/untracked. None is owned by this unit.
- Runtime: operator gate answered `go — Antigravity open; X4 not running; machine quiet`. No X4 process is present. Focused tests were run serially to avoid broad concurrent load.
- Focused baseline: CallModel `89/89`; LayoutProgram `648/648`; Scene `174/174`; PaintPlan `175/175`; CanvasRenderer `129/129`; PreviewPipeline `105/105` — total `1,320/1,320`.
- Frozen production preimages:
  - CallModel `1A3E372D8EFD3FF99F11BF58FFE9B9AEB9893A2F34E6773129B7456E5389AAA1`;
  - LayoutProgram `7CB407C64F5F5DA11532824EB27D00BD9CB5C18A0C967A503FDC57F82B57D1C0`;
  - Scene `90E14893F9B02F106E13834B2FFD07053A1BBBE71BFCCBBC6F70BE6191A61136`;
  - PaintPlan `8D1CC557EA95A1367495841C1FBC104D8DFF7A27FE9C8E3DE5D1651064810BE4`;
  - CanvasRenderer `C1A825B4B73ED669FBD45CDA689BA882CA2D2BA65FA196D8CED127A977C9F4FA`.
- Reproduced mismatch: retained X4 screenshots show content bands/widgets over the blurred game scene with no full panel texture or border. The retained Forge source-composition screenshot shows a thin frame-sized outline extending below the content.

## RECONCILE

- Resources/readers/writers searched: shipped Helper frame defaults/setters/descriptor emission; widget fullscreen background element activation; exact deployed `pipeline_test`; CallModel relevant-call/property owners; LayoutProgram frame facts; Scene frame completeness; PaintPlan unavailable-node emission; Canvas source-composition diagnostic strokes; SourceEditor presentation selection; relevant tests and Graphify reverse dependencies.
- Existing capability reused: the whole source-first pipeline already exists and the editor explicitly mounts `source-composition`. No renderer or route is missing.
- Causal current behavior:
  1. `pipeline_test` creates a frame but never calls `setBackground`, `setBackground2`, `setOverlay`, or `addFrameBorder`.
  2. Shipped defaults therefore leave all three texture icons empty; widget fullscreen deactivates all three elements.
  3. Forge currently lacks those exact layer facts and links every frame program gap into Scene completeness.
  4. The preview-only rawget Helper availability gap marks the frame partial even though its geometry and absence of texture paint are source-known.
  5. PaintPlan emits `unavailable-node` across the full frame; source-composition strokes that diagnostic, creating the false outline.
- Couplings checked: relevant-call closed domain; receiver tracking; source/color provenance; descriptor defaults; operation order; Scene issuance validation; Paint command order; Canvas presentation behavior; Preview identity; mounted SourceEditor selection.
- Extend-versus-replace: extend the existing owners. There is one pipeline and one Canvas renderer; adding a parallel implementation is forbidden.
- Capability-map delta: none at plan time. Promotion may record exact inactive frame-texture composition, but cannot promote icon raster or game blur parity.
- Plan change from prior B119 roadmap: frame texture activation and compositor semantics are now a separate source-port unit before broader pixel comparison. This follows the retained in-game evidence and corrects the previous invented `createFrameHandle.backgroundID/backgroundColor` visual assumption.

## IMPLEMENT

- Planned sequence:
  1. CallModel fail-first and exact tracked frame-setter records.
  2. LayoutProgram fail-first and ordered three-layer/default/backdrop descriptor projection.
  3. Scene fail-first and separate visual applicability versus nonvisual diagnostic completeness.
  4. Paint/Canvas/Preview fail-first proving exact inactive layers paint nothing while genuine visual uncertainty remains diagnosable.
  5. Final focused/dependency validation and receipt issuance.
- First candidate: ten existing source/test owners, 1,095 insertions / 42 deletions; native Luna implementation and independent review workers both completed and were closed. No candidate commit or external-record write.
- Scope changes and reasons: the 2026-08-31 audit requires corrective tests and repairs within the same owners. `x4UiPaintPlan.selftest.ts`, already permitted above, must now prove producer-side diagnostic classification; Canvas-only forged-marker tests are insufficient.

### 2026-08-31 corrective batch — SPECIFIED

One exact native Luna worker owns the existing eleven permitted source/test files. Sol owns documentation, runtime readiness, review, and promotion. Tests first must reproduce these findings before the corresponding production repair; retain red and green counts and do not rewrite fixtures into acceptance.

1. **P1 — frame setters rejected by Scene.** `SUPPORTED_OPERATION_KINDS` and operation owner validation omit the three new frame setters. Admit only exact source-proven frame-owned operations, and take each setter through the public pipeline. Wrong owners, malformed operations, and forged source/descriptor evidence must still refuse.
2. **P1 — property-table icon override dropped.** Helper assigns the positional icon, then applies properties. Add reciprocal source evidence for `properties.icon`; explicit empty overrides deactivate, explicit nonempty overrides activate, and dynamic properties make a possibly overridden icon unknown. Cover repeated pre-display replacements and do not preserve stale active/default facts.
3. **P2 — false outline returns when blur is false.** Inactive texture classification must not depend on backdrop being enabled. Suppress only an identified nonvisual cause; broad `data-flow`/`unavailable-node` categories cannot hide genuine visual uncertainty. Prove blur true/false, exact nonvisual gap, actual visual gap, unresolved nonempty texture, and unchanged diagnostic-map through the producer and Canvas.
4. **P2 — wrong source pins copied into the oracle.** Widget width/height fallback expressions are at lines **16977/16978**, not 16992/16993, under the pinned SHA-256 above. Check the actual shipped expressions as well as provenance; an oracle that duplicates a candidate constant is not source proof.
5. **P2 — second-profile and paint assertions too weak.** Run both retained profiles through public Paint/Canvas, prove actual content classes, and inspect the complete relevant path/trace for a panel-sized boundary. Add new frame-member/diagnostic-marker hostile-input coverage without weakening existing authority tests.

Profile reconciliation during visual preparation: `in-game-20260828/second-profile/runtime-receipt.json` and `pixel-measurements.json` identify the real drawable pair as **2544x1353 and 1920x1080, both at UI scale 1**. The roughly 1.25 measured ratio is drawable-height scaling, not a UI-scale override. Existing Preview fixtures at UI scales 1 and 1.25 remain useful regression controls but do not by themselves represent those retained X4 profiles. Keep them and add the actual drawable pair through public Preview/Scene/Paint/Canvas; mounted comparison uses the real pair. This clarifies criterion 8 without changing production scope.

Acceptance and out-of-scope boundaries are unchanged except for the explicit stronger evidence in criterion 8. Parent performs independent source/diff review, focused reproduction, canonical-corpus and serial host gates, and mounted visual inspection before promotion. No game/mod/config/corpus mutation, release, or claim of general parity is authorized by this corrective batch.

### 2026-08-31 F-6 exact-key correction — SPECIFIED

- Independent review rejected the corrected snapshot on one remaining source-law defect: `setBackground("positional-icon", { Icon = "" })` is incorrectly interpreted as an empty lowercase `icon` override. Helper's exact property metatable instead rejects `Icon`, preserving the positional icon. Arbitrary keys such as `bogus` are also silently omitted by Forge. Prior findings 1, 3, 4, and 5 have no remaining static blocker; finding 2 stays partial until this closes.
- Bounded implementation ownership: CallModel and LayoutProgram production/selftests plus PreviewPipeline selftest, all already inside the eleven-file unit. All other source files remain frozen. Reuse existing unsupported-property and source-evidence contracts; do not globally change normalization for unrelated widget families or introduce another parser.
- Tests first: case variants, underscore/space variants, arbitrary unsupported keys, and canonical exact names for all three frame setters. A wrong-case `Icon` must never deactivate a source-known positional icon; exact lowercase `icon` must still override it. Mixed valid/invalid fields retain source-backed valid facts and an explicit unsupported/gap result, never an invented accepted alias. Cover numeric/camel-case and color keys as well as icon, including a source-located public-preview regression.
- Acceptance: no invalid key becomes an applied descriptor fact or disappears from diagnostics; exact supported spelling and precedence remain intact; existing source/corpus/identity negative tests and two-profile Canvas evidence stay green. Record causal red before repair, final focused results, typecheck, exact-file lint, and final hashes. Any required wider edit must return to the parent before writing.
- Rollback remains the existing owned-file baseline; no game, mod, standing config, corpus, React, harness, release, Git, or external-record writes by the worker. Parent reruns acceptance gates against the final candidate rather than reusing previous-build success as proof.

## VALIDATE

### Additive UI-modding knowledge capture — documentation VERIFIED, 2026-08-31

- User request: retain short, practical X4 UI mistakes with a wrong example, a corrected example, and a few sentences explaining why. This is documentation work alongside the existing Luna correction, not a replacement or an expansion of production scope.
- Reconciliation: the existing owner is `F:\StarForge\wiki\x4-modding-methods\`; its two historical UI shelves already contain the column-refusal incident and three runtime UI traps. Preserve these records and add one concise quick-reference view under the existing Lua UI shelf, linked from the library and outcome index. Notion is a navigation/progress projection, not a new source of engine truth.
- Evidence: inspect X4 9.00 Helper/widget source, the historical incident records, the exact `pipeline_test` package and retained two-profile runtime receipts. Distinguish source-supported facts, historical game observations, current retained game evidence, and unknown limits. Do not promote the rejected Forge candidate as a successful method.
- Acceptance: each quick-reference entry has Wrong / Right / Why / Evidence; explanations stay short; examples use the real Helper API and respect its own scaling; every source path/range resolves. Correct the historical universal-column-cap overstatement without deleting its original incident evidence. No new game test is claimed.
- Validation/negative path: read back all edited Markdown, resolve links and source pins, check the new examples against the actual functions, and ensure the guide explicitly rejects both `13 columns always fails` and `preview green proves engine acceptance`. No capability-map delta for this documentation-only addition.
- Scope: the quick reference, existing library/outcome links and historical UI incident corrections, this plan/handoff, and existing external project records. Source review also found the older font example could double-scale normal Helper cells and its character-count wrap approximation is not exact; a dated correction must point readers to the new source-backed cards without deleting history. No game/mod/config/corpus, executable source, tests, skill files, or Codex memory writes. Rollback is removal of the newly authored guide plus reversal of only this task's Markdown hunks; preserve historical content and unrelated dirt.
- Implementation/readback: `07 UI (Lua widgets, menus, overlays)/ui-modding-gotchas-quick-reference.md` now contains 16 short cards over 230 lines, SHA-256 `C732FF0ADFC08F9402D5CBE97905EB40F3BF1E0FBA5CC19E25269A5AA3552644`. Library/outcome indexes link it; both historical incident records retain their observations and now identify superseded guidance.
- Validation: all 16 cards contain status, Wrong, Right, Why, and Evidence; every local source link resolves; Helper/widget hashes match the recorded corpus; actual source ranges were inspected; intermediate column counts and preview-not-engine-proof negatives are explicit. No new game test or execution of the illustrative snippets is claimed.
- External documentation projection: GitHub #41 comment `5474929529` and the existing Notion owner section were written/read back; the Current Status Google Doc was appended with a native date chip, unchanged tab `t.0`, and read-back revision `AIroW374UZQR2-mmuPwRP1cXNtVfyPPNXeNtmm9y8MPaKmviBMB0r-ycylI4K4mwSGZEjjdPOc8iPz0JMIxEEBS2DrXfaJSw9v5Zaue3dL1N`. These updates explicitly leave the renderer candidate rejected and B119 partial.
- Documentation close/AAR: `VERIFIED` for the bounded source-reviewed guide only; no capability-map delta. Triggered by correcting historical column/font/wrap overstatements and resolving an incorrect abbreviated skill-reference path. Sustain source-linked concise cards; improve discovery by resolving exact reference filenames before reading; highest evidenced risk is old confident guidance being reused after new source evidence disproves it. The dated correction banners and maintenance rule contain that risk without erasing history.
- Continuing source-review lesson, 2026-08-31: append card 17 on exact, case-sensitive Helper property names. The concise wrong/right pair uses `rotation_rate`/`Icon` versus `rotationRate`/`icon`; the property metatable at Helper 3534 rejects unrecognized keys. This records source truth only; it does not claim the Forge candidate has already been corrected or tested. Earlier 16-card hashes/external updates remain dated documentation evidence.
- Current guide SHA-256 after card 17: `024BE6EC40863FE506467BC0A364D47F5454F99A8EE233BE9CD12B58C98AD391`. The original six code-block cards label Wrong/Right as Lua comments, so a bold-label-only count is not a valid completeness test; validate both existing formats without rewriting the guide to fit the counter.

- Focused baseline -> PASS `1,320/1,320`.
- First candidate fail-first -> Preview `105/106`, Canvas `129/130`; final focused candidate `1,326/1,326` (CallModel 91, LayoutProgram 650, Scene 174, PaintPlan 175, Canvas 130, Preview 106), typecheck and exact-path lint green. These greens did not cover the audit escapes and are not acceptance.
- Corrective causal reds/greens and the dependency matrix -> recorded below. Independent review, serial E2E/precommit, and mounted visual evidence remain pending.
- Corrective intermediate readback, 2026-08-31: LayoutProgram reports `653` passed plus `1` explicitly skipped configured-source check (`654` total) with no corpus environment; this is not a substitute for the required configured-source run. Paint reports `179/180`, exit `1`, with the new `frame aggregate remains visual when Helper availability shares a genuine visual cause` regression red. Normal frame-size provenance is restored; mixed-cause classification remains in correction. No promotion from these intermediate results.
- Corrective terminal and coordinator reproduction, 2026-08-31: Carver completed and was closed with completed previous status. Its eleven-file final hash set matches the review snapshot at `03:49:00-04:00`. Coordinator's serial host run `03:51:06-03:53:03-04:00` passed all 19 existing X4 UI selftest entrypoints, including focused CallModel `91/91`, configured LayoutProgram `654/654` with zero skips and actual widget SHA/expressions checked, Scene `176/176`, Paint `180/180`, Canvas `132/132`, Preview `107/107` (`1,340/1,340` focused), SourceEditor canonical-color `12/12`, and EditorSession canonical-color `7/7`. Full matrix process exited `0`.
- Coordinator static gate, `03:55:50-04:00` -> `npm run typecheck` and exact eleven-file ESLint with `--max-warnings 0` both pass, exit `0`.
- Configured official census, started `03:56:54-04:00` -> PASS with explicit static gaps: selected/read/failed `81/81/0`, bytes `7,669,552`, applicable fatal `0`, warnings `29`, unverified files `70`, truncated files `26`, verification gaps `13,700`, exit `0`. Same ready manifest generation `1785035333079-2178b4c31f` and configured root; the six trusted-official restricted-online findings remain visible but non-applicable. The gap count increased by `19` from the previous checkpoint; this is not a fully verified vanilla corpus or an unchanged census.
- Isolated runtime oracle sweep -> `134/134` runtime-discovered oracles green, exit `0`. The owned oracle server stopped and port `8972` was checked clear before the next gate.
- Production build, `04:03:58-04:00` -> PASS, `1,848` modules, exit `0`; `dist/server.cjs` SHA-256 `0F9E7F028756A45CA6E4347A4B41B069D5D6A52ABA8674ECD8ACC383F5EEB98C`. The existing large-chunk warning remains advisory. No installed extension or store release was changed.
- E2E readiness attempt, 2026-08-31 -> did not start the browser suite. `Get-FileHash` could not read the installed sidecar's open `data/reference-manifest/index.db`; the baseline command exited `1` before test invocation. Do not stop the user's sidecar or classify this file lock as storage failure. For the next gate, protect all workspace/state/config files by checksum and explicitly exclude only the installed manifest's open SQLite database family from byte hashing. It is a cache, not the live workspace; its consistent read-only backup remains the isolated preview input. This is a recorded non-clean baseline-method change, not evidence that all installed cache bytes are unchanged.
- Mounted baseline launch attempt -> `BLOCKED`: `exec_command` rejected process creation with `blocked by policy` before the isolated Node/server launch and fixture seed. No process/session/PID or successful mutation receipt was returned. Parent configuration still reports Full access and approval-never; the cause of this discrepancy is not diagnosed. Respect the denial: no alternate launcher, encoded command, permission change, or worker bypass. Source correction may proceed inside its existing authority; mounted acceptance needs an approved launch path/operator action.
- Corpus harness selftest -> `12/12`; live census attempt -> `manifest-unavailable`, selected/read zero because no sidecar discovery existed. The subsequent startup command failed before launch due to invalid PowerShell `New-Item -LiteralPath`; no sidecar was started. This is readiness/tooling evidence, not a storage diagnosis.
- Readiness reconciliation -> the user's Antigravity IDE sidecar subsequently published port `52900`, PID `49756`. `/api/agent/schema` returned HTTP 200 and `/api/reference/status` returned ready generation `1785035333079-2178b4c31f` for the expected configured root. It is a user-owned process and must not be stopped as test cleanup. Both saved config hashes still match baseline; X4 remains absent.
- Isolated visual preparation -> the existing SQLite backup API opened the installed manifest read-only and completed a consistent `77,082`-page snapshot under `dev-docs/b119-x4-ui-pipeline-smoke/frame-composition-runtime-20260831/data/reference-manifest/index.db`; no scan or server launch occurred. New state/config/discovery/log roots are isolated, and config is empty.
- Native routing -> correction worker `01a05677-0aad-7fc1-8a90-5ab78ac88163` has recorded `thread_spawn` source, `luna_executor` role, exact `gpt-5.6-luna`, effort `max`, approval `never`, and `danger-full-access`; actual test-file edits and runtime progression are observed.
- F-6 terminal -> worker `01a056e6-bb20-7e52-8a04-39205ca48151` completed and was closed with completed previous status. Only the five declared files changed; Scene/Paint/Canvas and Preview/React production hashes remain frozen. Worker causal reds were CallModel `91/93`, configured LayoutProgram `654/655` with zero skips, and Preview `107/108`; final worker focused totals are `1,344/1,344`. A test-only typecheck error was corrected before its final green.
- F-6 independent parent review/reproduction, 2026-08-31 -> exact names now govern only the three frame-texture setter families; unsupported raw keys and source locations survive as gaps; valid properties retain source-backed values and precedence. The paired `Icon`/`icon` preview fixture traverses Scene/Paint/Canvas and checks the active-texture boundary. All five file hashes match the worker report. Focused parent results: CallModel `93/93` at `09:04:27`, configured LayoutProgram `655/655` with zero skips at `09:04:45`, Scene `176/176`, Paint `180/180`, Canvas `132/132`, Preview `108/108` by `09:07:10`; aggregate `1,344/1,344`. Typecheck, exact eleven-file zero-warning lint, and diff hygiene passed by `09:08:23` (all timestamps `-04:00`).
- Result-collection correction -> the first parent wrapper assumed every suite emitted JSON and stopped on Scene's text reporter, before later gates. It did not expose a product/test failure. The subsequent wrapper used the actual unique Scene/Canvas terminal summaries and JSON summaries for Paint/Preview; no test, threshold, or source was weakened. Preserve this triggered AAR rather than calling the first wrapper successful.
- Remaining UI matrix -> all thirteen other existing entrypoints exited `0` by `09:11:56-04:00`, including SourceEditor canonical-color `12/12` and shared integration `21/21`. Together with the six focused suites, all nineteen entrypoints passed against the F-6 candidate.
- Current F-6 precommit/build -> PASS: precommit completed at `09:19:20-04:00`, including action-receipt coverage `82` routes / `56` surfaces and final typecheck. Production build completed at `09:19:33-04:00`, `1,848` modules, exit `0`; server SHA-256 `124459D338EB4777F983770D80E08B256A51A15368D5186FEC2CA1EA39EFF27B`. The large-chunk warning remains advisory. PTY `1630` is completed.
- Current F-6 configured official census -> PASS at `09:24:33-04:00`, selected/read/failed `81/81/0`, bytes `7,669,552`, applicable fatal `0`, warnings `29`, unverified files `70`, truncated files `26`, gaps `13,700`, exit `0`. The configured root and ready manifest generation `1785035333079-2178b4c31f` were reused through the user-owned sidecar on `52900`. Six trusted-official restricted-online errors remain visible and narrowly non-applicable; they are not disabled for mod validation. PTY `83155` is completed.
- Guide structure recheck -> `17/17` complete cards, unchanged SHA-256 `024BE6EC40863FE506467BC0A364D47F5454F99A8EE233BE9CD12B58C98AD391`. An initial counter misplaced the colon in bold labels and reported eleven false omissions; the corrected check accepts the actual bold and Lua-comment formats without changing the guide or acceptance criterion.
- Current isolated oracle sweep -> `134/134` green, exit `0`, finished `09:27:40-04:00`. These existing isolated test harnesses do not retry or replace the blocked mounted-preview launch.
- Current tracked serial E2E -> PASS `103/103` across exactly `23` tracked specs at `09:35:15-04:00`, exit `0`. `test-results/e2e-verdict.json` generated `2026-08-31T13:35:15.492Z` proves complete `103` discovered/terminal tests, zero failures/flakes/skips, lifecycle/ownership complete, `treeGone=true`, and no remaining PIDs. User-owned sidecar PID `49756` is not in the captured test tree.
- Protected-data comparison -> NON-CLEAN; outer PTY `40581` completed exit `1`. Repository `.studio-state` and `data`, installed `state`, both configs, and exact retained Lua have identical before/after digests. Installed `data` retained `2,294` files / `295,674,675` bytes but changed aggregate SHA from `C827CB8B8C5AADCBC593D0AA0353937C378DCD1603B0B949F41ED7E17120553F` to `E1C7C9323C26D4279E574E9BCAAA717F80725C77237C7E5CAC84BFFDC4D99C82`, with only the previously declared open manifest SQLite family excluded. Do not call this full installed-data parity.
- Read-only drift investigation -> `[REPRODUCED]` the installed `runtime-debug-sessions/session-3420bacaf56ef63e3f1e6dea80f6dacc64c07b9087bc1dea0bb0dd0f0fb81e86.json` continues being atomically replaced after test teardown (`savedAt=2026-08-31T13:37:59.198Z` at `09:38:02-04:00`). It is the only file with a write timestamp in the inspected recent interval. The existing `RuntimeDebugSessionStore.persist` updates `savedAt` and writes through its configured data root. `[HYPOTHESIS, ~90%]` the user's running installed watcher explains this drift; the aggregate-only baseline cannot conclusively attribute every changed byte. No watcher was stopped, no new exclusion added, and no installed file was edited to obtain a green check. A future baseline should retain per-file digests and identify active diagnostic writers in advance.
- Final graph/identity readback -> deterministic Graphify refresh passed: `10,064` nodes / `25,297` edges / `325` communities; HTML is intentionally skipped above its existing size limit. PTY `45540` completed. All eleven candidate hashes and both forbidden Preview/React production hashes still match the reviewed snapshot. HEAD, tracking `origin/main`, and direct remote `main` remain `bd38ec6ca52fedc0db9e98be8e27be5c07b00b47`; index empty. No commit, push, release, or new game run occurred.
- Current external readback -> GitHub #41 comment `5479232070` matches the posted body exactly; issue remains open with 80 comments. Notion's targeted 17-card/F-6 sections read back with `Status=In Progress`, `Evidence Grade=Partial`, and both GitHub approval flags unchanged. Google Current Status tab `t.0` has five exact paragraph replacements at revision `ANLCKQlb1prs0EOymbsLBM255m6vOGVcjE_2Y_A1h9FfCZWNCFC9fUk3OJ6GCD_3R3hcPODcNK-H8QKHsZJfdZSybezJ2Ly5atvjpdU2gJTX`; text, normal paragraph styles, native GitHub/Notion links, body block count, pre-edit prefix and native date chip are preserved/verified. Trusted before/readback files live under `frame-composition-runtime-20260831/records/drive-f6-read-02/` and `drive-f6-readback/`. All three projections explicitly retain the policy blocker and non-clean installed-data checksum; none closes B119.
- Isolated package candidate, 2026-09-01 -> a disposable detached worktree at exact HEAD `bd38ec6ca52fedc0db9e98be8e27be5c07b00b47` received only the eleven reviewed B119 source/test diffs plus a package-only `0.0.69 -> 0.0.70` manifest identity so it can replace the already-installed `0.0.70` during local validation. Every owned candidate file matched the working review bytes; forbidden Preview production remained `50210E...196B1E` and React SourceEditor remained `83EE5D...DE642`. Configured focused validation passed `1,344/1,344` with LayoutProgram `655/655`, zero skips; typecheck, exact-file zero-warning lint, and diff hygiene passed. Root build passed at `1,848` modules with server SHA-256 `124459D338EB4777F983770D80E08B256A51A15368D5186FEC2CA1EA39EFF27B`.
- Packaged-product evidence -> fresh staging copied five bundle files, installed `169` runtime packages, stripped `72` vendor sourcemaps, retained the native SQLite binding, and found no staged secrets. The isolated staged product passed `16/16`, including canonical configured-corpus completion, protected config, supervisor ownership, parent-loss cleanup, and zero retained discovery. Inspector negatives passed `13/13`; final VSIX inspection passed `2,092` entries, `63,424,893` unpacked bytes and `18,594,756` archive bytes. Retained artifact: `vscode-extension/x4-forge-studio-0.0.70-b119-019fea10.vsix`, SHA-256 `32746D4B9AE81A7E7D4B12F53BE54CE015E50AA1229BCFD9ACAB6D179D2E789C`. The disposable worktree and both dependency junctions were removed; probe port `8982` is clear and user-owned `52900` / PID `49756` remained untouched. This is a local candidate, not an installation, visual pass, publish, or release.
- Packaged-candidate projection readback, 2026-09-01 -> GitHub #41 comment `5489961230` exists exactly once and names the package, hashes, green package probes, unchanged installed host, and remaining install/game gates. Notion page `3b84618e-d15b-8190-821e-c0eb96f43d5a` has the targeted packaged-candidate section read back at `2026-09-01T06:43:53.211Z`; it remains explicitly `PARTIAL`, with no property or approval change. Google Doc `17VLaIsT499KHg7zg30hOyLaBXB0-9jlrX3dQ63s3dtE`, tab `t.0`, was revision-guarded from `AIroW34DdVUM4arR1UclSIRSPX5VMiX9OetzX068fiz74H-nC2fVDM7r9EJBxw4wtRQFI6DHURWINhqaxwjJo6UPQaJDCeXuKmyjaAldrg05` to `AIroW36mAKtRPswKCXzEXeCA0CjLqXJBm2EKPdzV2umkk8z0PRJEMQJy4dNrv5VWOdzoNFLG1_weadJ5tI7Bc3QiZHkeu9ZhcJ-CWs6At1Z2`; both replacement paragraphs, normal styles, and native GitHub/Notion links were read back. All three durable projections say the running Forge was untouched and the candidate remains uninstalled and not verified in game.
- Read-only installed-product preflight, 2026-09-01 -> Antigravity IDE `1.107.0` exposes `C:\Users\Moshi\AppData\Local\Programs\Antigravity IDE\bin\antigravity-ide.cmd`; its live registry reports `x4forge.x4-forge-studio@0.0.70`. The candidate manifest has the same publisher/name/version and VS Code engine contract. Direct archive-to-installed streaming comparison found `2,090` candidate payload files: `1,987` byte-identical and `103` different or missing; the installed folder has `18` extras, including the old hashed JS/CSS pair and release-showcase images. Therefore installing this test candidate is a whole-extension replacement, not an eleven-file patch, and it is not suitable for OpenVSX publication in this form. The current installed folder has `2,105` files / `70,953,279` bytes and deterministic tree SHA-256 `3361AEDCEE5E71DAD46C2580BD5C1E92F18D1F1EBE6E689F4875A030CEEFD4C2`; `extensions.json` is `4A39D6BA...3D10` and `.obsolete` is `17487041...F102`.
- Authorized install/rollback procedure -> immediately before mutation, revalidate those three authorities and copy the complete installed extension folder, `extensions.json`, and `.obsolete` into one unique verified local temporary backup. Run the documented CLI with explicit extension directory, exact VSIX path, and `--force`; then reload the existing Antigravity window rather than launching another Forge. Verify the mounted server changed from `D27EDFA...3437` to `124459D...F27B`, the existing configured workspace/corpus still binds, and no second listener exists. The persistent global storage at `C:\Users\Moshi\AppData\Roaming\Antigravity IDE\User\globalStorage\x4forge.x4-forge-studio` is outside the replacement and must remain checksum-protected and unmodified. On any failed install or mounted acceptance, restore the complete folder plus both registry files from backup, reload, and prove the original tree/server hashes. This procedure remains gated on the operator's explicit `go`.
- Install-preflight projection readback -> GitHub #41 comment `5490092216` was written exactly once and read back. The Notion owner section was updated/read back at `2026-09-01T06:55:20.490Z` with counts, full rollback scope, and the OpenVSX exclusion. Google Current Status tab `t.0` was revision-guarded to `AIroW37d-IsKbJ6-mkmA1InI3tzjZWXoX8rSU0OZlVP8hNdUYr4lagkHtsJ4kPNv6twFuSa9ngEJU0SifYbgH41ISTygPWjQ1y_tJ3FHsVWv`; all three normal paragraphs and the package/preflight/Notion native links were read back. These are risk/bookkeeping corrections only; no install or acceptance state changed.
- Superseding release-surface candidate, 2026-09-01 -> the initial scoped package would have removed the installed showcase/release surface, so a native Luna worker rebuilt from exact HEAD plus only the eleven reviewed B119 files, the four current `0.0.70` wrapper files, and the exact fifteen installed showcase PNGs. Retained artifact: `vscode-extension/x4-forge-studio-0.0.70-b119-release-surface-019fea10.vsix`, `26,281,304` bytes, SHA-256 `19C547BE9633F102444113F805351764D6CCD7CC7635A6F2FABCA7E37E4E1D09`; inspector passed `2,107` entries / `71,561,839` unpacked bytes, selftest `13/13`, and the staged product probe `16/16`. Its `2,081/2,081` application files are byte-identical to the previously reviewed scoped candidate, embedded server SHA remains `124459D...F27B`, package semantics match installed `0.0.70` after removing installer `__metadata`, and all `15/15` showcase assets are byte-identical to installed. The prior `32746D...E789C` package is superseded for installation and remains only historical evidence.
- Parent package/containment readback -> independent inspection reproduced the exact artifact hash/size, `13/13` inspector selftest, final inspector PASS, `2,081/2,081` application-file equality, embedded server hash, semantic manifest equality, and `15/15` showcase equality. Direct comparison with installed found `2,004` identical same-path files, `98` byte differences, three archive-only paths, and three installed-only paths (installer manifest plus the two stale hashed assets). The complete-folder/registry rollback requirement therefore remains. Installed tree SHA `3361AEDC...D4C2`, server `D27EDFA...3437`, registry and `.obsolete` hashes remain exact; live Forge is still PID `49756` on `52900`, probe port `8982` is clear, and the disposable build root is absent. Evidence: `dev-docs/b119-x4-ui-pipeline-smoke/frame-composition-runtime-20260831/records/release-surface-package/receipt.json`.
- Release-surface projection readback -> GitHub #41 comment `5490501378` was written once and read back exactly. The existing Notion owner section was replaced in place and read back at `2026-09-01T07:33:38.746Z`; `Status=In Progress`, `Evidence Grade=Partial`, and both GitHub approval flags remain unchanged. Google Current Status document `17VLaIsT499KHg7zg30hOyLaBXB0-9jlrX3dQ63s3dtE`, tab `t.0`, was revision-guarded to `ANLCKQlnw-0h1u2Pv_Hony7A8gbnTUV9UxLzUKvdJ7RWeVbBgxaFeeIfYE4OISquE74UGWKb9XJ5QQ78828MdNiK3rWSpt99DwgMzixkAvC_`; all three target paragraphs remain `NORMAL_TEXT`, all four native links have the sampled style and exact URLs, and the existing date chip count remains one. All projections remain `PARTIAL / Not verified in game`; no install or acceptance state changed.

### 2026-09-01 installed-host registered-Lua materialization repair — SPECIFIED

- Installed-host baseline: the reviewed `0.0.70` candidate is mounted from `C:\Users\Moshi\.antigravity-ide\extensions\x4forge.x4-forge-studio-0.0.70`, sidecar `63125`, with the configured X4 9.00 corpus ready. Selecting `ui/addons/ai_influence_chat/aic_menu.lua` and exact target `menu.display` succeeds and reaches a read-only `rendered/current` source-preview canvas. The source session nevertheless remains `locked` with `missing-registered-lua` and `omitted-lua-source`, so source-safe editing, exact re-export authority, and shippability are unavailable; `Not verified in game` remains truthful.
- Reproduced cause: workspace `ws_f61166c42849c757cf219c37` contains the selected 68,082-character `aic_menu.lua` and root `ui.xml`, but registered `aic_uix.lua` is metadata-only (`omitted:true`, historical `523,783` bytes, `reason:too_large`). The source folder is `F:\DEV_ENV\projects\Mods\X4Mods\x4_ai_influence`; its current `aic_uix.lua` is `568,069` bytes. `server.ts` applies a generic `256 KiB` per-file inline cap before the fail-closed source-authority owner runs, so a valid registered UI source larger than that cap can never become an editable/shippable source bundle. This is an importer-policy defect, not renderer or Lua-parser evidence.
- Bounded unit: preserve the generic binary/unknown-file resilience caps while giving extension-root `ui.xml` and `ui/**/*.lua` a documented bounded text-source allowance sufficient for real X4 UI modules, still governed by the existing whole-import budget. Do not weaken `buildX4UiWorkspaceSource`: missing, omitted, duplicate, ambiguous, malformed, or unsafe registered Lua must continue to lock the bundle.
- Ownership: one native Luna owns only `server.ts` and `scripts/route-integration.mjs`. All B119 renderer, scene, paint, React, package, workspace-registry, and mod files remain frozen. The current user workspace is read-only during implementation; validation must use an isolated route fixture before any deliberate re-import or workspace switch.
- Acceptance: a route fixture with a registered UI Lua file above `256 KiB` and below the new bounded UI-source limit imports exact bytes and is not marked omitted; an unrelated text file at the same size retains the prior omission behavior; over-limit registered UI Lua remains tracked/omitted; binary and total-budget protections remain effective. Existing import/deploy route checks, typecheck, exact-path lint, and the relevant source-authority tests must pass. Negative-path behavior cannot be weakened to make the selected target render.
- Rollback: revert only the two owned-file hunks. No current workspace, mod source, installed extension, game directory, standing config, release, or external record is mutated by this corrective unit. A fresh package/install is separately gated after review.
- Read-only causal hydration: an in-memory clone of the exact current workspace was passed through the unchanged source-authority owner. Before hydration it was `locked`, `shippable:false`, with registration resolutions `resolved/resolved/missing-file/resolved`. Supplying only the omitted `aic_uix.lua` text from the configured source folder changed the clone to `source-owned`, `shippable:true`, with all four registrations `resolved`; exit `0`. No workspace or disk bytes were changed. This proves the missing materialization is sufficient for authority without weakening the fail-closed owner; it is not a safe re-import or a current-source hash claim.
- Candidate behavioral checkpoint: route integration passes `496/496`, typecheck exits `0`, and WorkspaceSource remains `5/5`. The requested file-wide zero-warning lint command is not an applicable clean baseline: independent current-versus-HEAD ESLint JSON is exactly `server.ts 241/241`, `route-integration.mjs 0/0`, and `modCompiler.ts 1/1`, all with zero errors and zero messages in the candidate line ranges. Existing warning debt is retained visibly; no warning was introduced or suppressed.

### 2026-09-01 package-readiness parity-fixture correction — SPECIFIED

- Baseline command: direct invocation of `runPackageReadinessParitySelftest()` returns `8/9`, exit `1`. Its `addTable(24)` row is green and proves a source-located blocking `x4-ui.add-table-column-limit` error before package output. The only red row, `readiness_preserves_x4_warning_without_blocking`, still uses `table:addRow(..., { height = 200 })` and expects a row-budget warning.
- Reconciliation: the current source-backed linter deliberately reports that Helper ignores `addRow` height; `x4UiLint.selftest.ts` proves this plus the correct production-shaped large cell-height warning. The parity fixture is stale, not evidence that the linter or package blocker should change.
- Bounded correction: one native Luna owns only `src/lib/modCompiler.ts` and may change only the stale selftest fixture/assertion so it exercises a source-valid large cell height while preserving the intended nonblocking package-warning projection. Production readiness logic, linter rules/severities, compiler output, and all other files are frozen.
- Acceptance: causal `8/9` red is retained; final direct parity selftest is `9/9`; `x4UiLint.selftest.ts`, typecheck, diff hygiene, and changed-range ESLint parity pass. `addTable(24)` remains a blocking error with its whole-frame failure mode and exact path/line. The unchanged file-wide warning is reported rather than repaired outside scope. Rollback is the one fixture hunk.
- Candidate result: direct parity is `9/9`, X4 UI lint is `140/140`, typecheck and diff hygiene pass, and the blocking `addTable(24)` row remains green. The owned file has one unchanged pre-existing `no-explicit-any` warning at line 123; HEAD has the same one warning and the candidate range has none. Acceptance is therefore changed-line clean with honest file-wide debt, not a fabricated zero-warning file claim.
- Parent combined-tree verification, 2026-09-01 -> route integration `496/496`, typecheck exit `0`, package-readiness parity `9/9`, X4 UI lint `140/140`, WorkspaceSource causal matrix `5/5`, and named diff hygiene all pass. The route stack exited normally after its isolated production-server checks. The only diff-hygiene output is Git's existing LF-to-CRLF notice for `scripts/route-integration.mjs`; there is no whitespace error.
- Installed-host causal baseline, 2026-09-01 -> a protected, read-only `POST /api/agent/mod-folder/import` against the configured real `x4_ai_influence` folder returned HTTP `200` without replacing the user workspace. The unchanged source-authority owner then reported `locked`, `editable:false`, `shippable:false`, reasons `missing-registered-lua` and `omitted-lua-source`; current `aic_uix.lua` was tracked as `too_large`, `568,069` bytes, with zero materialized content bytes. This is the exact post-install causal oracle: the repaired packaged host must return `source-owned` and `shippable:true`, not merely HTTP success. The boundary remains `Not verified in game`.
- Package-gate causal rejection, 2026-09-01 -> the first materialization artifact passed route `496/496`, staged-product `16/16`, inspector `13/13`, and exact archive checks, but it is rejected for installation because isolated precommit stopped at the durable-writer fingerprint gate. A fresh detached replay proved the source inventory and LF-normalized bytes match current main after all 33 overlays, while the audit still hashes `048363c26377139f0a1849c9c35318ddb54d69eac05e7e15c811c197e7ab227b` instead of declared `05690bb5f10e2db021404ccac32a259f4372c1ddaad91303d7c9a1e029c41ab5`. Exact delta: seven context slices, one in `AgentBridge.tsx` and six in `instanceDiscovery.ts`; the scanner slices the raw CRLF/LF text before whitespace normalization, so equivalent checkout line endings shift the context boundary. This disproves the earlier sequencing hypothesis and requires an EOL-invariant audit plus regression test before rebuilding; no installed, game, mod, or user-workspace bytes changed.

### 2026-09-01 installed green2 checkpoint — VERIFIED BOUNDED HOST / FULL B119 PARTIAL

- The EOL-invariant writer audit repair and exact reviewed overlay passed complete precommit, routes `496/496`, production build, staged-product probe `16/16`, inspector matrix `13/13`, and independent 2,107-entry archive inspection. Retained package: `vscode-extension/x4-forge-studio-0.0.70-b119-source-materialization-green2-019fea10.vsix`, `26,281,393` bytes, SHA-256 `057E5193FF35112F4A1978C291C1BCE371504CC1A139EA41B672CB19FE696CDF`; embedded server SHA-256 `626C651742402EC4C04FD7FEA4A2FD3190ADDC389170581C5688250165E67314`.
- The package is now installed at `C:\Users\Moshi\.antigravity-ide\extensions\x4forge.x4-forge-studio-0.0.70`. The mounted managed sidecar is PID `38296` on `127.0.0.1:60956`, and its installed `app/dist/server.cjs` matches the embedded server hash above. Antigravity shows the real installed HUD & Lua UI SourceEditor, the configured X4 9.00 corpus, and the permanent `Not verified in game` state. No parallel Forge server is involved.
- A fresh read-only import of configured `x4_ai_influence` materialized `ui.xml` and all seven registered Lua sources. The former 568,069-byte `aic_uix.lua` omission is gone; source authority is `source-owned`, `editable:true`, and `shippable:true`, with no generated collision. The already-mounted persisted workspace still contains its historical omitted-file snapshot and remains locked until a deliberate re-import; this stale workspace state is not evidence against the fresh import oracle and was not replaced during inspection.
- Strict configured MENU/HUB/COMM census executed against exact pinned source hashes and passed Scene `176/176`. It also sets the next fidelity boundary: MENU currently yields 16 Scene cells / 3 widgets / 5 texts / 7 glyphs, while HUB yields 4 / 0 / 0 / 0 and COMM 3 / 0 / 0 / 0. All three remain `partial`, renderable, and `Not verified in game`; a passing census freezes current behavior but does not satisfy the brief's three-menu few-pixel parity requirement.
- No current mod/game byte was written, X4 was not launched, and the user canvas was not replaced. The next implementation unit must first issue a compact causal gap receipt for the missing HUB/COMM visible operations, then port one shipped Helper/widget behavior through the existing CallModel -> LayoutProgram -> Scene -> Paint -> Canvas chain. OpenVSX remains forbidden until the user-facing release surface and game comparison are accepted.
- Repository/source close: checkpoint `71828d9c99383dea5da89b5c56d15575839f4f88` is pushed with local, tracking, and direct-remote parity. GitHub #41 comment `5503331754` was written and read back exactly; the existing Notion owner section was replaced in place and read back with `IN PROGRESS / PARTIAL / Not verified in game`; Drive tab `t.0` is revision-guarded at `AIroW36RDRS6r8agSVm85k-cm_eTPmdWhnXT6l_qtFAUYt9t74oSmZ7bk2Z0tRsKaWgzrp1cnxcphhEmJHu-IFWzHmV9oAqsNqq6wvHSBiAW`.
- Drive trusted-read evidence: bridge `3.6` persisted the exact pre-write document/control/outline/text artifacts under `C:\Users\Moshi\.codex\visualizations\2026\08\10\019fea10-33af-7f32-8175-424ddfebda4e\b119-drive-trusted-read-20260901-r1\`; no protected controls were detected and the existing native date element was preserved. Post-write readback verified the updated executive/current-work/confidence paragraphs, the new `HEADING_2`, exact source hash, `18`-card KB count, one GitHub checkpoint link, and two Notion-owner link occurrences.

### 2026-09-01 local `Helper.scaleFont` wrapper projection — SPECIFIED

#### PLAN / acceptance contract

- Bounded unit: carry one exact same-file local numeric wrapper result through the existing CallModel -> LayoutProgram -> Scene -> Paint chain when the wrapper source proves the guarded `Helper.scaleFont(font, size)` call and same-parameter fallback used by the configured HUB/COMM sources. This extends existing owners; it does not add a renderer, evaluator, Lua runtime, or guessed geometry.
- Authoritative references:
  - shipped `helper.lua` 846-858: `Helper.scaleFont` returns `math.ceil(fontsize * Helper.uiScale)` unless scaling is explicitly disabled;
  - shipped `helper.lua` 5249-5261 and 5390-5400: row height is the maximum participating cell offset plus cell height, and zero-height text delegates to its minimum text height;
  - configured `aic_hub.lua` 98-102 / use at 679 and `aic_comm.lua` 74-78 / use at 505;
  - the existing exact local-function declarations, local-invocation identities, numeric resolver, canonical Zekton metrics, Helper layout kernel, and public editor-session pipeline.
- In scope: exact source-proven wrapper recognition/resolution; descriptor provenance; fail-closed negative variants; focused fixture coverage; a deliberate configured HUB census revision caused by the newly known font/text geometry; and an explicit COMM no-visual-delta receipt if runtime-composed text still prevents exact C++ text height.
- Out of scope: generic Lua execution, arbitrary `pcall`, arbitrary local-function return inference, C++ frame acceptance, runtime strings, loop expansion, texture/material parity, menu-content reconstruction, game/mod/config writes, OpenVSX publication, or a claim of universal 1:1 parity.
- Likely owned paths: `src/lib/x4UiLayoutProgram.ts`, `src/lib/x4UiLayoutProgram.selftest.ts`, and `src/lib/x4UiScene.selftest.ts`. `src/lib/x4UiCallModel.ts` / `.selftest.ts` may be added only if the worker proves the existing immutable declaration/invocation contract cannot carry a closed source descriptor; all other production/test files are frozen pending parent re-plan.
- Risk: loose text matching could silently execute an unrelated helper and create persuasive false geometry. Recognition must be exact, source-contained, same-file, same-declaration, direct-binding, single finite literal argument, and anchored to the guarded return/fallback structure. Unknown or mutated shapes remain explicit gaps.
- Authorization boundary: user authorized Forge updates, Computer Use, game launch, commits/pushes, and unattended validation. This unit still does not mutate the game, mod, current Forge workspace, or installed extension until source review and focused gates pass. X4 remains closed until a materially changed installed preview exists.
- Rollback/checkpoint: HEAD/origin `305865806eaf91cb446927e599b8ffc8ad969b8a`; restore only the explicit owned hunks. Existing 49 unrelated dirty paths, the persisted stale workspace, installed sidecar PID `38296` / port `60956`, retained package, mod corpus, and game files are preserved.

Acceptance criteria:

1. A same-file direct invocation resolves only when its exact tracked declaration has one parameter and source proves the configured guarded shape: protected direct `Helper.scaleFont` call using that parameter, success/number/positive guard returning the protected result, then fallback returning the same parameter.
2. The wrapper result uses the already-ported Helper 9.00 scale law and current projection `uiScale`; no font or geometry constant is guessed. At scale 1, `font(18)` and `font(13)` resolve to 18 and 13. Non-unit-scale fixtures prove the wrapper's `math.ceil(size * uiScale)` and then preserve the widget's separate shipped scale pass in `getTextHeight` / `createDescriptor` (helper.lua 5482-5497 and 5540-5545). The renderer must mirror that double scaling rather than silently correct the mod source.
3. Provenance retains invocation, declaration, argument, and shipped Helper source identity. The value is never labeled a source literal or preview sample.
4. Wrong callee/receiver, wrong parameter, extra side effect, changed guard, changed fallback, multiple/dynamic argument, alias/rebind ambiguity, recursive/nested wrapper, malformed source range, stale model identity, and forged public evidence remain unresolved or are rejected by the existing authority validators.
5. The causal red fixture proves current `font(size)` is unresolved, which makes the zero-height title text height and parent row geometry unavailable and suppresses otherwise-known buttons. The green HUB fixture proves the correction through public LayoutProgram -> Scene -> Paint, not a private helper alone. COMM must remain unavailable if its independent runtime-composed text prevents exact C++ text-height evidence.
6. Exact configured HUB/COMM source hashes remain unchanged. The revised HUB census must add only geometry/widgets/text/glyph/paint made reachable by this resolved wrapper; unrelated operations and current looped HUB tab-button gaps remain visible. COMM may resolve the font fact while retaining zero widgets and its row-geometry gap. MENU must not regress.
7. Existing `Not verified in game`, partial-status, linter, source-authority, corpus-identity, and game-acceptance boundaries remain unchanged.

Required validation and negative path:

- Retain a fail-first causal receipt before repair and final exact counts after repair.
- Run `npx tsx src/lib/x4UiLayoutProgram.selftest.ts`, strict configured `npx tsx src/lib/x4UiScene.selftest.ts`, and any directly affected focused suite identified by Graphify/reverse import review.
- Run typecheck, exact changed-path ESLint with baseline comparison, `git diff --check`, source/hash/forbidden-owner scans, configured corpus gate, aggregate B119 matrix, build, serial E2E, and precommit before promotion.
- Package/install only after source review and full static gates. Then inspect the mounted real SourceEditor at 1920x1080 and 2560x1440/source-equivalent profile; launch X4 only if the Forge candidate visibly improves and is worth a controlled comparison.
- Negative receipt must show every near-miss wrapper stays unresolved and that no unknown text height is converted into a row rectangle merely to expose buttons.

#### BASELINE / RECONCILE

- Baseline source checkpoint: HEAD = origin/main = `305865806eaf91cb446927e599b8ffc8ad969b8a`; index and the likely owned files were clean before this plan edit. X4 is not running; the existing installed Forge is mounted and its current workspace is preserved.
- Strict configured baseline remains Scene `176/176`: HUB Layout `18` operations / `11` applied / `4` cells / `16` gaps, Scene `4` cells / `0` widgets / `0` texts / `0` glyphs / `32` gaps, Paint `47` commands / `38` diagnostics; COMM Layout `14/12/3/11`, Scene `3/0/0/0/24`, Paint `36/30`.
- Reproduced causal receipt: CallModel parses the HUB/COMM title option tables as static objects, and the CLOSE/DOSSIER/END cells already have known content kind, width, standard button height `25`, text, font size `9`, and active state. They do not render because the preceding title `font(18)` / `font(13)` result is discarded as `local helper return value is not inferred`; zero-height text then lacks a canonical final height, the row rectangle is unavailable, and Scene records `cell parent row/table geometry is unavailable` for every cell in the row.
- Reconciliation correction from an independent in-memory direct-call oracle: replacing only the HUB wrapper use with exact `Helper.scaleFont("Zekton", 18)` reaches 2 Scene widgets (title text and CLOSE), 3 text nodes, and 17 glyphs. Doing the corresponding COMM replacement still reaches zero widgets because the title string itself is runtime-composed and its exact C++ text height remains unavailable. This unit must not turn that second, independent uncertainty into guessed row geometry.
- Reconciliation decision: do not relax Scene geometry or invent a minimum row rectangle. The shipped scale law is already exact in `x4UiLayoutKernel.ts`; repair only the missing source-proven local-result bridge in the existing LayoutProgram path. No capability-map delta at specification time.

#### IMPLEMENT — 2026-09-02 source-proven wrapper checkpoint

- `x4UiLayoutProgram` now recognizes only the exact configured one-parameter local wrapper: a direct consumed literal invocation whose same-file declaration calls active `Helper.scaleFont('Zekton', parameter)` inside global `pcall`, accepts only `ok and type(v) == 'number' and v > 0`, returns `v`, then falls back to the same parameter. Invocation, declaration, argument, protected Helper call, active Helper identity, and model source ranges remain coupled.
- The resolved local result uses the already-ported Helper 9.00 `math.ceil(size * uiScale)` law, then remains subject to the widget descriptor's separate shipped font-scale pass. At `uiScale=1.25`, the configured wrapper result becomes `23`/`17`, and the later descriptor path produces `29`/`22`; Forge does not silently repair the mod's deliberate double scaling.
- Recognition fails closed for wrong receiver/callee/parameter/guard/fallback, side effects, dynamic or multiple arguments, nested/recursive functions, malformed or forged locations, Helper alias ambiguity, parameter/result-name collisions, and same-file global-authority mutation before the consumed invocation. The authority predicate covers `pcall`, `type`, `rawget`, and `Helper` through `_G`/`_ENV` member or indexed writes, uncertain dynamic keys, `rawset`, and global/local `_ENV` replacement. Equivalent single/double quoted strings and numeric zero spellings retain literal semantics.
- Configured HUB gains only the title/button geometry made reachable by this fact. COMM remains at zero widgets because its runtime-composed title still prevents exact C++ text-height evidence; no row rectangle is guessed to expose the later buttons. MENU is unchanged.
- Independent review found parameter/result collisions, global builtin rebinding, vacuous negatives, and quote/numeric literal-equivalence gaps in the first candidate. That correction retained a causal `661/665` red receipt with exactly four failing checks before its `666/666` green. A final zero-write audit then found the global-environment authority family. Tests-first expansion produced causal reds `671/682` (11 failures), `672/688` (16), `672/691` (19), `674/697` (23), and, after the first repair, `_ENV` replacement red `699/705` (6). Twenty-nine attacks reproduced in total. The final fail-closed matcher passes `705/705`, zero skips; `39` authority probes classify as unavailable (`33`) or unsupported model shape (`6`), with no known font leakage. Production changed only after each causal red.

#### VALIDATE — 2026-09-02 local acceptance and package

- Focused/source matrix under bundled Node `24.19.0`: CallModel `93/93`; LayoutProgram `705/705`, zero skips; strict configured Scene `176/176` with MENU/HUB/COMM `3/3` actually executed; PaintPlan `180/180`; CanvasRenderer `132/132`; PreviewPipeline `108/108`; SourceEdits `83/83`; UI integration `21/21`; configured corpus `81/81/0`, fatal `0`.
- Host gates: typecheck passed; exact changed-file ESLint passed with zero errors/warnings; named diff hygiene passed; complete precommit passed; production build emitted `1,848` modules; prior tracked-only serial E2E passed `103/103` with zero failed, flaky, bad, or quarantined results. Graphify refreshed after the authority repair to `10,101` nodes / `25,416` edges / `325` communities; HTML visualization was correctly skipped above its 5,000-node safety limit while `graph.json` and `GRAPH_REPORT.md` updated. Protected `.studio-state`, `data`, config/token, and pre-existing Git path sets remained unchanged.
- Superseding production assets: browser JS `dist/assets/index-BQaOS9Gd.js`, `2,699,055` bytes, SHA-256 `93F4A3BDC45B346C00E13FBEEDAD69F150C974066C24FB9F8BC0DDE7073E6A9C`; CSS `index-C-3vqpSY.css`, SHA-256 `2E442EBF0DC7CC7381FAAC208B1F58D6716B0E18D48E941A48136CF487781F08`; unchanged frontend-only server SHA-256 `626C651742402EC4C04FD7FEA4A2FD3190ADDC389170581C5688250165E67314`. Mounted proof must therefore use the served browser asset and visible behavior, not a server-hash transition.
- Superseding isolated package: `vscode-extension/x4-forge-studio-0.0.70-b119-font-authority-019fea10.vsix`, `26,283,699` bytes, SHA-256 `3C3F9FC16C269A43D91B5C298A3D6E48A1BD0800DDCD90A7E1AB80096E9FDE9F`; final inspector `2,107` entries / `71,568,514` unpacked bytes; staged product `16/16`; inspector selftest `13/13`; all `15/15` showcase assets present; native `better_sqlite3.node` present; zero forbidden secret/state/config/log/sourcemap files. The embedded browser asset exactly matches `93F4A3BD...6A9C`. The prior `font-wrapper` package and receipt remain historical evidence for the reverted attempt and are superseded for installation.
- Installed acceptance attempt: a complete verified backup remains at `C:\Users\Moshi\AppData\Local\Temp\x4forge-b119-font-wrapper-install-backup-20260902T070241727Z-ac8989d224a247d08515dc4da5c48c35`. The earlier `font-wrapper` package's `antigravity-ide --install-extension ... --force` failed before replacement after `1,099` `EPERM` rename retries because the running IDE held a native module. The attempt is `REVERTED`: current installed payload versus backup is `2,106/2,106` files, `71,559,305` bytes, zero content mismatches; `extensions.json` and `.obsolete` match the backup hashes; protected config `1/1` and state `13/13` have zero mismatches. The restored host serves old `index-BKURy_nX.js`, SHA-256 `77473900F7DD747A14A45442DFAD77A2B81B8E3EAB4EEFF9EABB49C99281E015`; superseding `index-BQaOS9Gd.js` is absent. X4 and ports `3000/3001/3100/3101` are absent.
- The existing Antigravity window was reloaded only to prove the original extension survived, and the retained screenshot still displays `Not verified in game`. Physical Escape then stopped Computer Use; no further UI automation, shutdown, installation, game launch, or workspace mutation was attempted. Full Antigravity shutdown is required before a later reversible install can mount the corrected native-bearing extension.

## REVIEW

- Source truth -> reconciled and specified.
- Independent zero-write audit `01a053f1-cd2e-7c23-a1de-7e5065274031` -> `FINDINGS`, five reproduced defects above. The candidate is rejected; criterion 7 missed, 1-6 and 8-11 partial, 12 satisfied. Auditor terminal result captured and worker closed with completed previous status.
- Corrected implementation and negative paths -> F-6 independently reviewed against shipped Helper and reproduced by the parent; no remaining blocker found in that five-file delta. Current-snapshot broad/runtime/visual gates remain separate acceptance requirements.
- Final acceptance walk -> criteria 1-7 and 9-10 are source-reviewed and causally tested; criterion 8 now has an installed mounted SourceEditor inspection and both retained-profile operation traces, but not three-menu few-pixel parity; criterion 11 has green current source/static/corpus/oracle/build/E2E/package/installed-host materialization evidence, while the broader historical installed-data census remains non-clean; criterion 12's truthful `PARTIAL / Not verified in game` boundary is retained. No required failure or unavailable method has been waived.
- Independent zero-write reviewer `01a056cb-03ac-7f12-824c-08dfb2e6e37c` (Singer) returned `FINDINGS` (F-6 above); all eleven final hashes matched its supplied baseline. It ran no tests and changed no files, then was closed with completed previous status. Runtime metadata confirmed native `thread_spawn`, exact `gpt-5.6-luna`, max, `never`, and `danger-full-access`. Earlier green gates do not override its source-law finding.
- Parent review during the corrective batch reproduced two remaining source/oracle defects before promotion: Paint's new blur-false fixture explicitly expected the still-wrong visual full-frame diagnostic, and `resolveFrameDimension` incorrectly relabelled normal frame default geometry with texture-fallback source pins. The active worker was given exact line/function corrections: test the same nonvisual cause through both blur states, retain real visual uncertainty, and keep normal-frame versus texture fallback provenance separate. Existing greens are not accepted over these findings.
- Review tooling -> `reviewctl` and the skill's named BlastRadius script are unavailable. Use the existing Graphify graph, complete owned diff, independent source audit, and deterministic regression gates; do not install a parallel review stack. Graphify's current owner path links CallModel and Canvas through the SourceEditor selftest/corpus-authority seam; live source inspection governs the unindexed new helpers.
- 2026-09-02 wrapper review close -> the first final reviewer returned `FINDINGS` because global environment authority was not source-proven. A tests-first Luna correction reproduced `29` attacks, repaired only those shared source shapes, preserved six already-unsupported parser/model cases as unsupported rather than claiming a repair, and returned `PASS` after Layout `705/705`, strict Scene `176/176`, exact lint, diff hygiene, and unchanged worktree-scope evidence. Acceptance criteria 1-7 now pass at source/static/package level. Mounted superseding-product pixels and current X4 comparison remain unavailable. No criterion was weakened, no capability-map delta is claimed, and the overall result remains `PARTIAL / Not verified in game`.

## CLOSE

- Status: `PARTIAL / INSTALLED-HOST SOURCE-MATERIALIZATION VERIFIED`; source/static/corpus/oracle/build/E2E/package gates and the installed fresh-import authority pass. Mounted current-workspace pixels are inspected, but that persisted workspace predates the repaired import and three-menu parity is still open. No capability-map promotion to 1:1 rendering.
- Superseding 2026-09-02 checkpoint: `PARTIAL / SOURCE + PACKAGE VERIFIED / CORRECTED PACKAGE UNMOUNTED`. The prior installed source-materialization milestone remains valid for its exact old bytes; the new wrapper candidate is source-reviewed and package-ready but not mounted. The reversible install failed before replacement, rollback is re-proven byte-for-byte, and the backup is intentionally retained. No OpenVSX or new X4 result is claimed.
- Source and projection readback: commit `7aa5b9d50fd91eede47ab28fd96fada0b163d936` is pushed with exact local/tracking/direct-remote parity. GitHub #41 comment `5507614879` was read back exactly. The Notion owner section was replaced in place and read back at `2026-09-02T09:44:45.771Z` with `Status=In Progress`, `Evidence Grade=Partial`, and approval properties unchanged. Google Current Status document `17VLaIsT499KHg7zg30hOyLaBXB0-9jlrX3dQ63s3dtE`, tab `t.0`, was revision-guarded to `AIroW34DU6xn-NC50YIedCZy5EIcoGLThl-pxkEMaLd-UwQAXpF1mRISy9ICP58Fblsq2movES5ILGuMoSoq27rjQpAEc7SxbpN8e4Amy_qa`; heading/body styles, exact native links, and the existing single date chip were read back. Every projection remains `PARTIAL / Not verified in game` and calls the package unmounted.
- Remaining risks/deferred work: HUB/COMM visible-operation coverage, MENU hierarchy completion, texture/material and border/bracket paths, dynamic backdrop, exact two-profile pixel comparison, keep-outs, AI Influence reconstruction, universal X4 acceptance, and OpenVSX release.
- Immediate resume boundary: preserve the restored installed payload, retained rollback backup, and user's persisted workspace. Current user authorization permits the bounded IDE/game sequence. Fully exit every Antigravity process, install the exact superseding `font-authority` VSIX, restart the existing IDE, prove the served browser asset is `93F4A3BD...6A9C`, and visually inspect HUB/COMM before deciding whether X4 comparison is warranted. A window reload is insufficient while native modules are locked. Keep `Not verified in game` authoritative.
- Historical blocked audit, 2026-08-31: the explicit parallel-launch rejection persisted in turns `01a056d4-531b-7d52-a62e-56f14527ad2e`, `01a057e7-31af-7072-ba96-c82aef7a5017`, and `01a05818-5339-7740-9e7a-ff4bb2a126a5`. The operator then supplied the already-mounted Forge as a corrected route. This resumes the goal and starts a fresh blocked audit. The old turn count must not be reused. The feature remains `PARTIAL`; current-candidate visual proof now depends on a reversible install/reload, not a second server.
- Current source checkpoint commit: `feat(ui-editor): port source-proven font wrappers` (`7aa5b9d50fd91eede47ab28fd96fada0b163d936`). Suggested record-close commit title: `docs(b119): record font-authority checkpoint`.

## AAR

- Triggered: reconciliation corrected the prior frame visual-property assumption; fresh review found five behavior/oracle defects despite focused greens, followed by exact-key finding F-6, requiring reimplementation. Runtime setup failed before launch, an open SQLite cache defeated the initial checksum method, and a later process-launch request was explicitly rejected by tool policy.
- Sustain: compare the exact deployed source, shipped Lua, retained Forge image, and retained X4 image before modifying renderer behavior.
- Improve work/approach: prior source-composition checks proved diagnostics were non-opaque but not visually applicable. New setter positives must reach downstream acceptance, source-pin tests must inspect shipped expressions, and each claimed profile must actually render.
- Improve tools: retain compact exit/count/failure summaries using each suite's actual reporter format; assuming all reporters are JSON or misreading Markdown label punctuation creates false collection failures. One multi-file documentation patch also failed its context check before mutation and was split into exact-file edits. Use a consistent SQLite snapshot or an explicit cache-only exclusion for an open manifest; permission-neutral worker metadata does not guarantee that a separate process launch is permitted.
- Packaging follow-up: committed HEAD still declared `0.0.69` while the user-installed and dirty release projection is `0.0.70`. The candidate therefore used a documented package-only version adjustment instead of absorbing unrelated release/showcase files. The first configured-suite wrapper omitted `X4_REFERENCE_ROOT` and honestly reported one skip; rerunning with the saved corpus produced `655/655`, zero skips. A composite cleanup command was policy-rejected before deletion; explicit junction identity checks and registered-worktree cleanup then succeeded without touching the dependency targets.
- Install-preflight correction: matching manifest identity does not mean matching installed payload. The scoped candidate differs from or lacks `103` installed paths and would remove `18` installed extras on a clean replacement. Treat it only as a reversible local acceptance candidate with a complete folder/registry backup; a later release must reconcile and build from the real release source state rather than publish this package.
- Release-surface correction: package acceptance must compare the installable surface, not only the reviewed application subtree. The first scoped artifact passed product probes while omitting fifteen installed release assets; the superseding candidate preserves them without changing any of the `2,081` reviewed application files. This removes the known showcase-loss defect but does not weaken the whole-folder rollback requirement or make the local acceptance package publishable.
- Highest-risk evidenced weakness: a nonvisual source-analysis gap can currently become persuasive full-frame paint. This unit must separate diagnostic truth from visual applicability without hiding either.
- Installed-host follow-up: a successful package/import fix can coexist with a stale persisted workspace that still reports the old omission. Always distinguish a fresh import response from the workspace currently mounted in the editor; replacing the latter is a separate user-state mutation.
- Census follow-up: a green exact-count test can freeze an incomplete renderer. HUB/COMM's zero visible text/widgets are accepted baseline evidence, not parity evidence; subsequent work must improve a causal source-law path and revise the census deliberately.
- External-record tooling follow-up: the first combined Notion fetch exceeded the model-output budget, and the first two standard-isolate trusted-read loads lacked `atob` / `TextEncoder`. Re-fetching the Notion resource alone and using the bridge's documented `encodeURIComponent` byte-count method produced exact read/write/readback evidence without bypassing the guard. Keep rich connector payloads on disk and return only bounded excerpts.
- Global/project lessons banked: the existing workflow/project AAR ledgers now record exact-key normalization, reporter/patch friction, and respecting the launch denial. Guide card 17 is source-supported, not a claim of a new game result. Repeated bookkeeping mistakes triggered a degradation call; finish this checkpoint without another implementation batch. No universal method is promoted from partial validation.
- 2026-09-02 wrapper AAR: sustain exact AST/source identity plus mutation-sensitive near misses; review must challenge binding collisions, direct builtins, environment tables, `rawset`/`rawget`, dynamic keys, and `_ENV` replacement rather than textual shape alone. The first final audit correctly stopped promotion despite green tests; `29` attacks then reproduced and justified the bounded repair. Improve the install path by requiring a full IDE process stop before same-version replacement of a native-bearing extension; repeated reloads cannot release extension-host DLL locks. Tool friction included two retained E2E temp roots, one parent PowerShell parse failure, one policy-rejected broad cleanup before exact safe cleanup, one detached configured-census miss, the `EPERM` install rejection, a failed non-required package-script probe, a malformed process regex, and physical Escape stopping the earlier Computer Use run. Highest-risk evidenced weakness remains a source-correct package being mistaken for a mounted result; browser asset hash plus real rendered-host inspection is the required boundary. Guide card 19 records the source-proven double-scale wrapper lesson without claiming new game proof.
