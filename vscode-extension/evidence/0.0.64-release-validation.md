# X4 Forge Studio 0.0.64 release validation

Status: `BLOCKED` — OpenVSX accepted publication, but its scan/activation pipeline has not exposed a public version
or archive. Final post-upload review also found and removed a newly generated blank line at changelog EOF; the clean
source now differs by one trailing newline byte from the immutable accepted package input. Public parity and the
publish-before-commit exact-source close cannot proceed.

Superseded close: stable `0.0.65` repaired the generator and reached verified public/download/installed parity.
`0.0.64` remains immutable blocked evidence and was not republished.

## Baseline

- Public latest before publish: `0.0.63` (`2026-07-31T06:57:28.681951Z`).
- Target `0.0.64` before publish: direct endpoint HTTP `404`; absent from the versions listing.
- Local package and lock versions before bump: `0.0.63`; target was the unique next stable patch version.
- The gitignored `.env.local` contained a nonempty `OVSX_PAT` entry. Its value was never printed or persisted.

## Release metadata

- `vscode-extension/package.json`, lockfile top-level version, and lockfile root package version are exactly `0.0.64`.
- Curated `release-notes.json` contains ten modder-facing bullets for `0.0.64`.
- Generated `CHANGELOG.md` starts with `## 0.0.64 — 2026-08-07`; its ten bullets are exactly equal and ordered the
  same as the curated release-note array.
- Changelog generator selftest: `8/8` PASS. JSON/version/equality readback: PASS. The worker's bounded owned-diff
  check did not catch the generator's extra blank line at file EOF; final full `git diff --check` after publication
  reproduced `vscode-extension/CHANGELOG.md:510: new blank line at EOF`. Removing that one byte makes final
  `git diff --check` PASS, but archive inspection proves `extension/changelog.md` in the accepted VSIX still ends in
  `0A0A` while clean source ends in `0A`; this is an exact-input mismatch, not a runtime-code mismatch.
- Stable channel: package command did not use `--pre-release`; package manifest has no
  `Microsoft.VisualStudio.Code.PreRelease` property and `package.json` has no true preview flag.

## Build, package, inspection, and probe

- Root `npm run build`: PASS; Vite transformed `1,828` modules and built the production client/server bundle.
- `npm run changelog`: PASS; `53` versions, newest `0.0.64`.
- `npm run stage-app`: PASS; `169` runtime packages, native binding present, vendor sourcemaps stripped, no secrets.
- Extension `npm run build`: PASS; fresh controller and supervisor outputs.
- Stable package: `vscode-extension/x4-forge-studio-0.0.64.vsix`.
- Archive inspection: PASS; `2,091` entries, `61,446,705` unpacked bytes, `18,098,219` archive bytes.
- Local VSIX SHA-256: `925616AD141D45AC75E8AD90E5A21FD73F0DE0DADC6CA0B5B23C7D4C11F03057`.
- Embedded package/manifest version: exact `0.0.64`; pre-release property absent.
- Staged-sidecar probe: `16/16` PASS, including native server boot, authenticated/public boundary, real corpus
  completion, exact owned PID, orphan exit/discovery cleanup, invalid-parent refusal, and force reap without parent
  termination.
- Critical staged hashes match the installed validated feature build:
  - server: `28D789465936D5869DD3707E21821CF0697FB2FE5851DC5034E5C3F9DD685BD7`;
  - extension: `6821A0DF527D69831DD8A7F821A74B50165361C0CA68CAE90758DAD53F026A6E`;
  - supervisor: `436D6BC63DC72D79B8F4A811DE7C8EE8D3EA4532A6280D010504E451F714FAC9`.

## Publication

- Exact command result: `Published x4forge.x4-forge-studio v0.0.64`, exit `0`.
- The token was loaded into one in-memory PowerShell variable from `.env.local`, never echoed, and cleared after the
  command.
- Initial post-publish verification showed latest remained `0.0.63`, the specific `0.0.64` endpoint returned `404`,
  and the versions list omitted `0.0.64`.
- Cache-bypassed public checks returned origin `MISS` responses; the guessed archive URL was also `404`. A
  publisher-authenticated metadata GET remained `404`, and the token was cleared immediately afterward.
- OpenVSX's public status page reported all services operational, and its search API showed unrelated releases being
  activated after this upload. Current registry source confirms that newly uploaded versions remain inactive while
  asynchronous required scanners run; fallback completion runs every five minutes, default polling can span about
  thirty minutes, and default scan timeout is sixty minutes. Scan details are exposed only through an admin API.
- A final bounded monitor performed `60` cache-bypassed checks every thirty seconds from
  `2026-08-07T07:15:54Z` through `2026-08-07T07:45:57Z`. Every check returned latest `0.0.63` and exact-version HTTP
  `404`; the monitor ended `MONITOR_TIMEOUT` without one activation. This followed earlier bounded checks beginning
  immediately after publication, so the candidate remained unavailable through the registry's default scan-timeout
  horizon.
- Do not republish. Resume only if OpenVSX exposes the exact `0.0.64` endpoint and public archive, or if registry
  administration supplies a scan disposition. Because `0.0.64` is occupied and its accepted archive contains the
  pre-cleanup changelog input, a clean public close also requires fixing deterministic changelog EOF generation and
  producing a new monotonic package version; the accepted `0.0.64` archive must remain immutable. Completion requires
  public bytes/hash equality before final precommit, commit, push, or release-ledger claims.

## Containment

- No game or mod directory was written or exercised.
- No standalone app, end-user CLI, alternate runtime, or pre-release channel was introduced.
- Unrelated dirty and untracked owner paths remain outside release staging.
