# X4 Forge Studio 0.0.65 release validation

Status: `VERIFIED` — the stable package was generated deterministically, passed the full local and installed-product
contract, was published once, and the public OpenVSX download is byte-identical to the inspected local candidate.

## Baseline and recovery scope

- Pre-release repository baseline: `HEAD == origin/main == c334127888368993cf350e13839376a0d44507d4`.
- Public latest before this release: `0.0.63`; exact `0.0.65` returned HTTP `404` immediately before upload.
- OpenVSX had previously accepted `0.0.64` without activating it. That version was not retried or replaced.
- The bounded recovery reused the existing release pipeline and repaired only its changelog EOF contract. No Forge
  runtime behavior, game/mod data, standalone app, end-user CLI, alternate runtime, or pre-release channel was added.

## Native implementation and release metadata

- Native `luna_executor` routing used exact `gpt-5.6-luna`, reasoning effort `max`, and no parent-turn fork. The worker
  owned only `scripts/gen-changelog.mjs`, `package.json`, `package-lock.json`, `release-notes.json`, and generated
  `CHANGELOG.md`.
- `buildChangelog()` now normalizes LF output, removes every terminal newline before appending exactly one `\n`, and
  the entrypoint writes those exact bytes without appending another newline.
- Changelog selftest: `12/12` PASS, including exactly-one-terminal-LF, no-double-LF, no-CR, and deterministic-output
  cases. `npm run changelog` generated `53` versions with `0.0.65` first.
- Package, lock top-level, and lock root versions are all exactly `0.0.65`.
- Curated and generated `0.0.65` notes contain the same `11` ordered, modder-facing bullets. The generated file starts
  with `## 0.0.65 — 2026-08-07`, ends in exactly one LF, contains no CR, and passes `git diff --check`.
- Product-copy/forbidden-reference scan: `0` banned mentions. Stable-channel readback found no preview flag or
  pre-release manifest property.

## Build, package, inspection, and probe

- Root `npm run build`: PASS; Vite transformed `1,828` modules and produced the fresh production client/server bundle.
- Fresh `npm run stage-app`: PASS; `169` runtime packages, native binding present, `72` vendor sourcemaps stripped,
  and no package secret detected.
- Extension build and stable VSCE packaging: PASS.
- Exact archive: `vscode-extension/x4-forge-studio-0.0.65.vsix`.
- Archive inspection: PASS; `2,091` entries, `61,446,861` unpacked bytes, `18,098,264` archive bytes.
- Local VSIX SHA-256:
  `ACBF40475A0AB55AA269E5728FE2B0927C22C9B9CC1F38F12AAD473A1F392D21`.
- Staged-sidecar probe: `16/16` PASS, including real corpus completion, exact process ownership, orphan discovery and
  cleanup, invalid-parent refusal, and force-reap containment.
- Critical staged hashes:
  - `app/dist/server.cjs`: `28D789465936D5869DD3707E21821CF0697FB2FE5851DC5034E5C3F9DD685BD7`;
  - `out/extension.js`: `6821A0DF527D69831DD8A7F821A74B50165361C0CA68CAE90758DAD53F026A6E`;
  - `out/sidecar-supervisor.js`: `436D6BC63DC72D79B8F4A811DE7C8EE8D3EA4532A6280D010504E451F714FAC9`.

## Full repository gate

- `npm run precommit:check`: PASS, exit `0`, `492.9s`, final `[precommit] OK`.
- Included E2E-verdict selftest `54/54`, product-copy guard, durable-writer selftest `14/14`, capability contract
  `11` capabilities / `294` disposed routes / one dynamic registrar / `10` MCP aliases, MCP capability policy,
  receipt coverage `82` routes / `52` surfaces, typecheck, mirror, size, and tripwire gates.
- Final synchronized `npm run precommit:check` after the durable close records: PASS, exit `0`, `465.4s`, terminal
  `[precommit] OK`. The commit hook reruns the same gate against the exact staged checkpoint.

## OpenVSX publication and public parity

- Pre-publish readback at `2026-08-07T08:18:39Z`: latest `0.0.63`; exact `0.0.65` HTTP `404`; credential present but
  never printed.
- One upload only: `Published x4forge.x4-forge-studio v0.0.65`, exit `0`, at
  `2026-08-07T08:19:28.9813238Z`. The credential was held in memory, redacted from captured output, and cleared.
- No second upload was attempted. Seventeen bounded public checks observed normal indexing lag; at
  `2026-08-07T08:23:04Z`, latest became `0.0.65` and the exact endpoint returned HTTP `200`.
- Public metadata: namespace `x4forge`, name `x4-forge-studio`, version `0.0.65`, registry timestamp
  `2026-08-07T08:19:28.378329Z`.
- Public archive URL:
  `https://open-vsx.org/api/x4forge/x4-forge-studio/0.0.65/file/x4forge.x4-forge-studio-0.0.65.vsix`.
- Public download: `18,098,264` bytes, SHA-256
  `ACBF40475A0AB55AA269E5728FE2B0927C22C9B9CC1F38F12AAD473A1F392D21`; byte count and hash both exactly match the
  immutable local candidate.

## Installed Antigravity proof

- Antigravity CLI install: PASS; `x4forge.x4-forge-studio@0.0.65` is the active installed version after a real
  `Developer: Reload Window` host reload.
- Installed root:
  `C:\Users\Moshi\.antigravity-ide\extensions\x4forge.x4-forge-studio-0.0.65`.
- Installed server, extension, and supervisor byte counts and SHA-256 values exactly match the inspected staged
  package hashes above.
- The reloaded extension started `node.exe` from the installed `0.0.65` root and owns the managed sidecar on
  `http://127.0.0.1:52634`.
- Rendered Forge `v1.0.428` shows the preserved workspace, graph and project validation active, Mission Director
  schema `1,507` elements, AI-script schema `1,408` elements, and `2,333` script properties loaded. Durable screenshot:
  [0.0.65-installed-antigravity-forge-v1.0.428.png](0.0.65-installed-antigravity-forge-v1.0.428.png), `240,099`
  bytes, SHA-256 `FE26D7B81141970F3E80C82C1CEE2133797B04B6CD41B318D6BC75BA96AF0715`.
- Live read-only receipt selftest: HTTP `200`, `pass=true`, `allPassed=true`, `25/25`.
- Live schema: HTTP `200`, `3,409,071` characters; it advertises `POST /api/agent/bulk-transform/apply`, the required
  `x-forge-operation-id` header, and the exact caller-owned bounded-operation authority text.
- `F:\DEV_ENV\X4_Forge\config.json` remained byte-identical before install and after live validation at SHA-256
  `ABEC6AE6AD169392878E06E19346C5E85C1DFB5D9BDFACDD80BA77DAF227C697`.

## Negative paths, rollback, and boundary

- The immutable `0.0.64` archive was rehashed unchanged and never republished.
- Package inspection rejected no secret, version, channel, native-binding, manifest, or containment gate. One
  supplemental broad secret regex overmatched dependency test-data paths; the authoritative inspector and refined
  package-root scan both found zero secret-bearing package roots.
- No game or mod directory was written, validated, deployed, or exercised. No unrelated dirty path entered package
  staging.
- Rollback is reinstalling retained
  `vscode-extension/x4-forge-studio-0.0.63-busy-liveness-20260806.vsix` with `--force`, then reloading Antigravity.

## Review and close

- Requirements 1-6 of the recovery plan are done and evidenced. Requirement 7's final repository/GitHub parity is
  performed by the release commit containing this evidence and its post-push owner-issue readback.
- Release artifact status: `VERIFIED`. W3B1a remains `VERIFIED` at `5/5`, while W3B1/W3 and the overall extension-native
  implementation program remain `IN_PROGRESS / PARTIAL`.
- Commit subject: `release: publish X4 Forge Studio 0.0.65`.
